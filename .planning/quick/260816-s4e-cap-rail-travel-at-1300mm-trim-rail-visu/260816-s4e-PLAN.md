---
phase: quick
plan: 260816-s4e
type: execute
wave: 1
depends_on: []
files_modified:
  # modified — enforced travel range + grid-alignment invariant
  - src/kinematics/rail.ts
  - src/kinematics/rail.test.ts
  - src/trajectory/mode-rail.test.ts
  # modified — fixed-span visual rail track
  - src/scene/RailRig.tsx
  - src/scene/rail-rig-geometry.test.ts
  # modified — stale literal sweep
  - src/ui/manual-jog.test.ts
  - src/scene/camera-defaults.ts
  # DELETED — superseded footprint-measurement approach
  - src/scene/robot-footprint.ts
  - src/scene/robot-footprint.test.ts
autonomous: false

requirements:
  - SCENE-04
  - DASH-03

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "U-1: The enforced rail travel range is exactly -1.3 m to +1.3 m. Typing a rail value beyond that into the Dashboard clamps to the end-stop, exactly like every other out-of-range clamp in the app — the robot can no longer be commanded into the visually-broken region the user reported."
    - "U-1: A toolpath-resolved rail position can never exceed the new range either — `resolveRailPosition` only ever scans and returns candidates inside `RAIL_TRAVEL`."
    - "U-2: The rendered rail track spans exactly 2.8 m — from `RAIL_CENTER_X - 1.4` to `RAIL_CENTER_X + 1.4` — a fixed, user-specified figure, with the end-stop blocks sitting at the new travel limits."
    - "U-2: No part of the rail track's length is derived from a measurement of the robot's rendered footprint any more; that approach is fully removed, not layered over."
    - "U-3: `MODE_RAIL_START_OFFSET_M` is an exact whole multiple of `resolveRailPosition`'s candidate spacing BY CONSTRUCTION (derived from it, not restated as a literal), so the invariant cannot silently break again under any future travel-span change."
    - "U-3: The mode-station grid-alignment property is asserted at a tolerance orders of magnitude tighter than one grid step — a resolved station that drifted by a fraction of a step would now fail, where the previous assertion would have passed."
    - "U-4: No module under src/ still hardcodes the superseded travel bound, the superseded total span, or the superseded candidate-spacing figure — every consumer derives from `RAIL_TRAVEL` / `RAIL_CANDIDATE_SPACING_M`."
    - "U-4: `src/scene/robot-footprint.ts` and its measuring test are gone (no remaining importer), OR — if a legitimate importer is found — they are kept and that importer is named explicitly in the SUMMARY."
    - "U-5: The g-code import to toolpath-playback pipeline, per-mode upload/auto-load, manual-input drift fix, sliders, Home/Reset button and ModeBar layout are all untouched and still green."
  artifacts:
    - src/kinematics/rail.ts
    - src/kinematics/rail.test.ts
    - src/scene/RailRig.tsx
    - src/scene/rail-rig-geometry.test.ts
  key_links:
    - "`rail.ts` RAIL_TRAVEL -> `src/ui/manual-jog.ts` railLimitsMillimetres/clampRailMillimetres -> DashboardPanel field + slider min/max (already fully derived — the enforced clamp follows automatically, no Dashboard edit needed)"
    - "`rail.ts` RAIL_TRAVEL -> `RailRig.tsx` end-stop mesh X positions AND `src/collision/pose-collision.ts` end-stop AABBs (one shared definition)"
    - "`RailRig.tsx` TRACK_LENGTH -> `pose-collision.ts` trackXMin/trackXMax rig-collision box AND `CellScene.tsx` floor plane via RIG_FOOTPRINT_WIDTH"
    - "`rail.ts` RAIL_CANDIDATE_SPACING_M -> MODE_RAIL_START_OFFSET_M -> railStartXForMode -> toolpath-anchor mode stations -> compiled trajectory railPos"
---

<objective>
Cap the rail's ENFORCED travel range at ±1.3 m, trim the rendered rail track to a fixed ±1.4 m about the rail centre, and resolve every ripple effect of the smaller span — the grid-alignment invariant on `MODE_RAIL_START_OFFSET_M`, the stale hardcoded bound/span/spacing literals, and the now-orphaned footprint-measurement module.

Purpose: this is the user's THIRD report of the same rail visual defect (`Robot Problem.png` at the project root). Quick 260816-nup grew a cosmetic margin; quick 260816-qym derived the track length from a measured robot footprint. Both were confirmed by the user NOT to have fixed it. The user has now determined the correct numbers empirically through live testing and supplied them exactly. This plan implements those numbers precisely — it does NOT re-derive them, does NOT re-investigate root cause, and does NOT layer onto the superseded footprint approach.

Output: a rail that clamps at ±1.3 m the way every other bounded input in this app already clamps, a visually sensible 2.8 m track, an invariant that holds by construction rather than by coincidence, and no orphaned dead code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260816-qym-fix-off-by-one-manual-input-bug-root-cau/260816-qym-SUMMARY.md

@src/kinematics/rail.ts
@src/kinematics/rail.test.ts
@src/scene/RailRig.tsx
@src/scene/rail-rig-geometry.test.ts
@src/scene/robot-footprint.ts

**Pre-established facts (already verified during planning — do NOT re-derive):**

- `RAIL_CENTER_X` is `(min + max) / 2` and stays exactly `0` under the new symmetric range, so a track mesh of width `TRACK_LENGTH` centred at `RAIL_CENTER_X` spans exactly `±TRACK_LENGTH/2`.
- `src/store/cellStore.ts` `setManualRailPos` already routes through `clampRailPosition`, and `src/ui/manual-jog.ts` already derives its millimetre limits from `RAIL_TRAVEL`. **No store or DashboardPanel edit is needed** for the enforced clamp — it follows automatically from the constant. Those files are OUT OF SCOPE (manual-input drift fix / sliders / Home-Reset are confirmed working).
- `src/collision/pose-collision.ts` imports `TRACK_LENGTH` and `RAIL_TRAVEL` from the scene/kinematics modules. Its track box is an *intrusion* volume: a SHORTER track makes that box strictly smaller, so it can only ever flag FEWER rig collisions — no previously-valid pose becomes invalid. It does NOT import `TRACK_OVERHANG` or `TRACK_END_MARGIN`.
- The only importers of `src/scene/robot-footprint.ts` are `src/scene/RailRig.tsx`, `src/scene/rail-rig-geometry.test.ts`, and its own `robot-footprint.test.ts` — all three of which this plan rewrites or deletes.
- `src/trajectory/mode-rail.test.ts` recomputes the candidate spacing locally from `RAIL_TRAVEL` / `RAIL_RESOLUTION_CANDIDATES`, so it self-updates; it is included here only to switch it onto the newly exported spacing constant so there is ONE derivation.

**Arithmetic already worked out (use these, do not recompute from scratch):**

| Quantity | New value |
|---|---|
| Travel span (`max - min`) | 2.6 m |
| `RAIL_RESOLUTION_CANDIDATES` | 201 (UNCHANGED) |
| Candidate spacing (`span / 200`) | 0.013 m |
| Whole steps nearest the previous mode-station intent | 46 |
| `MODE_RAIL_START_OFFSET_M` (46 × spacing) | 0.598 m |
| Remaining travel beyond a mode station, tight side | 0.702 m |
| `TRACK_LENGTH` | 2.8 m (ends at `RAIL_CENTER_X ± 1.4`) |
| Visible track beyond each end-stop | 0.1 m |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap enforced rail travel and make the mode-station grid alignment hold by construction</name>
  <files>src/kinematics/rail.ts, src/kinematics/rail.test.ts, src/trajectory/mode-rail.test.ts</files>
  <behavior>
    - `RAIL_TRAVEL.min` is `-1.3` and `RAIL_TRAVEL.max` is `1.3`; the total span is 2.6 m.
    - `clampRailPosition` clamps to the new limits (existing implementation is already fully derived — no logic change).
    - A new exported `RAIL_CANDIDATE_SPACING_M` equals `(RAIL_TRAVEL.max - RAIL_TRAVEL.min) / (RAIL_RESOLUTION_CANDIDATES - 1)`.
    - `MODE_RAIL_START_OFFSET_M / RAIL_CANDIDATE_SPACING_M` is a whole integer to within 1e-9 — the grid-alignment invariant, now structural.
    - For a symmetric point cloud that resolves exactly to `RAIL_CENTER_X`, the same cloud translated by `±MODE_RAIL_START_OFFSET_M` resolves to the baseline plus that offset to within 1e-9 (NOT merely within one grid step).
    - Both mode stations still lie strictly inside the travel range with positive remaining travel in both directions.
  </behavior>
  <action>
    In `src/kinematics/rail.ts`:

    1. Set `RAIL_TRAVEL` to `{ min: -1.3, max: 1.3 }`. Update the module header's provenance note: the travel range is no longer a "deliberately-chosen plausible round number" — it is now the range the user established empirically through live testing of the rendered rig (quick 260816-s4e), because beyond it the robot renders in a visually broken state. Keep the existing honesty convention (state plainly that it is still not a hardware datasheet value), and update the total-span figure stated in that header.

    2. Immediately AFTER the existing `RAIL_RESOLUTION_CANDIDATES` declaration, add and export `RAIL_CANDIDATE_SPACING_M`, computed as the travel span divided by `RAIL_RESOLUTION_CANDIDATES - 1`. Document it as the single derivation of `resolveRailPosition`'s candidate spacing, so no consumer restates it.

    3. Replace the `MODE_RAIL_START_OFFSET_M` literal with a derivation: a module-private `MODE_RAIL_START_OFFSET_STEPS = 46` (whole candidate steps) multiplied by `RAIL_CANDIDATE_SPACING_M`, yielding 0.598 m. This is the fix for the invariant the shrunken span broke — express the offset IN grid steps so exact alignment is true by construction and cannot silently regress under any future span change. Rewrite doc bullets (b) and (c): (b) the station now sits at roughly 46% of the 1.3 m half-travel, leaving 0.702 m of travel beyond it on the tight side, so both stations stay well inside the end-stops; (c) exact grid alignment is now structural rather than a coincidence of two independently-chosen numbers, and state explicitly WHY it matters (a resolved station lands exactly on the centred result plus the offset instead of drifting by up to half a step).

    4. Do NOT modify `resolveRailPosition`'s candidate loop body — leave its `t = i / (RAIL_RESOLUTION_CANDIDATES - 1)` parametrisation exactly as-is. Changing it would perturb float results that other suites assert against, for no behavioural gain.

    In `src/kinematics/rail.test.ts`:

    5. Update the travel-span assertion to the new total span (keep it reading `TOTAL_TRAVEL`, derived from `RAIL_TRAVEL`, not a restated bound), and update that test's own name.

    6. Import `RAIL_CANDIDATE_SPACING_M` and add a `describe` block for the grid-alignment invariant with two cases: (i) `RAIL_CANDIDATE_SPACING_M` equals the span divided by `RAIL_RESOLUTION_CANDIDATES - 1`; (ii) `MODE_RAIL_START_OFFSET_M / RAIL_CANDIDATE_SPACING_M` differs from its own rounded integer by less than 1e-9.

    7. Strengthen the existing translation-covariance test: it currently tolerates one full grid step, which is exactly the weak tolerance that would let a broken invariant pass unnoticed. Replace the local `gridStep` computation with the imported constant, tighten the tolerance to 1e-9, and rename the test to state that the translated cloud resolves EXACTLY onto a candidate, not merely near one. If this fails, the correct response is to fix the offset derivation — do NOT widen the tolerance back toward a grid step.

    In `src/trajectory/mode-rail.test.ts`:

    8. Replace the locally-recomputed `GRID_STEP` with the imported `RAIL_CANDIDATE_SPACING_M` (one derivation, not two). Keep that suite's `<= GRID_STEP` tolerance as-is — it compiles a REAL toolpath whose worst-case-reach optimum need not coincide with a candidate, so a grid step is the honest bound there.

    **Prose caution for every comment you rewrite in this task:** state the NEW figures only. Do NOT restate the superseded bound, superseded total span, or superseded candidate-spacing figure anywhere in these files' prose — Task 3's sweep gate asserts those numerals are absent from `rail.ts` and `rail.test.ts`. Refer to them by name ("the superseded range", "the previous spacing") instead of by numeral.
  </action>
  <verify>
    <automated>npx vitest run src/kinematics/rail.test.ts src/trajectory/mode-rail.test.ts</automated>
  </verify>
  <done>The enforced travel range is -1.3 m to +1.3 m; `MODE_RAIL_START_OFFSET_M` is derived as a whole number of candidate steps (0.598 m); both new invariant cases and the tightened 1e-9 covariance assertion pass; both bundled samples still compile to status `ready` at both mode stations in `mode-rail.test.ts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trim the rendered rail track to a fixed 2.8 m span and retire the footprint-derived overhang</name>
  <files>src/scene/RailRig.tsx, src/scene/rail-rig-geometry.test.ts</files>
  <behavior>
    - `TRACK_LENGTH` is exactly 2.8; the track's ends sit at `RAIL_CENTER_X - 1.4` and `RAIL_CENTER_X + 1.4`.
    - `TRACK_OVERHANG` is 0.1 — derived as the fixed half-span minus half the travel span, i.e. the visible track remaining beyond each end-stop.
    - `RailRig.tsx` imports nothing from the footprint-measurement module.
    - The floor plane still fully contains the track, and its half-width still covers the travel limit plus the robot's reach envelope.
  </behavior>
  <action>
    In `src/scene/RailRig.tsx`:

    1. Delete the `ROBOT_FOOTPRINT_HALF_WIDTH_X` import and the entire quick-260816-qym commentary block that explains the measured-footprint derivation (the paragraph about the mounting plate being the wrong object, the arm's worst-case reach, and the margin reduction rationale). That whole approach is superseded — remove it, do not annotate it.

    2. Add `export const TRACK_HALF_SPAN_M = 1.4` with a doc comment recording its provenance honestly: a fixed figure specified directly by the user from live testing of the rendered rig (quick 260816-s4e), chosen so a modest amount of visible track remains past each end-stop and no more. State plainly that it is NOT derived from the robot's rendered extent and must not be re-derived from one — the two previous attempts to size this from geometry were both rejected by the user.

    3. Redefine `TRACK_LENGTH` as `TRACK_HALF_SPAN_M * 2`.

    4. Redefine `TRACK_OVERHANG` as `TRACK_HALF_SPAN_M` minus half the travel span (i.e. minus `(RAIL_TRAVEL.max - RAIL_TRAVEL.min) / 2`), keeping it exported and documenting it as "visible track remaining beyond each end-stop block", which is now a consequence of the fixed span rather than an input to it.

    5. Delete the `TRACK_END_MARGIN` export entirely — it has no meaning under a fixed span, and no module outside this file and its geometry test consumes it (verified during planning). Update the file-header export-list comment (the one naming which constants `src/collision/pose-collision.ts` derives its envelope from) so it no longer advertises the removed constant.

    6. Leave every other constant and the JSX untouched. In particular: the twin track meshes stay centred at `RAIL_CENTER_X` with width `TRACK_LENGTH` (this is what makes the span land exactly on the fixed half-span), the end-stop blocks stay at `RAIL_TRAVEL.min`/`RAIL_TRAVEL.max` (which now sit at the new limits automatically), and `RIG_FOOTPRINT_WIDTH` keeps deriving the floor from `TRACK_LENGTH` — the floor shrinking alongside the track is the intended, derived consequence, not a regression.

    7. Record in a short comment that at a travel extreme the carriage plate's outer edge now sits slightly beyond the visible track end (the plate is wider than the remaining visible track), and that this is a direct consequence of the user-specified numbers — flagged for the human checkpoint in Task 4, NOT to be "fixed" by growing the track.

    In `src/scene/rail-rig-geometry.test.ts`:

    8. Drop the `robot-footprint` import. DELETE these three now-contradictory assertions outright rather than weakening them (each one asserts the opposite of the new spec and cannot be satisfied by it): the "track extends at least the measured footprint plus margin past the limit" case, the "TRACK_OVERHANG is strictly greater than the superseded carriage-derived figure" regression guard, and the "TRACK_LENGTH is strictly longer than the carriage-only-just-fits case" case. Replace the file's header comment so it describes what the suite now proves: a fixed, user-specified visible span.

    9. Rewrite the travel-bound assertions to the new limits, and add: `TRACK_LENGTH` is 2.8 to 10 decimal places; the track's low end is `RAIL_CENTER_X - TRACK_HALF_SPAN_M` and its high end `RAIL_CENTER_X + TRACK_HALF_SPAN_M`, both matching `RAIL_CENTER_X ∓ 1.4` to 10 decimal places; `TRACK_OVERHANG` is 0.1 to 10 decimal places and equals `TRACK_HALF_SPAN_M` minus half the travel span.

    10. Keep the "floor footprint still fully contains the track" case, and add one guard that the floor's half-width is at least `RAIL_TRAVEL.max + ROBOT_REACH_ENVELOPE` — so shrinking the track can never leave the robot standing off the floor plane.

    **Prose caution for every comment you rewrite in this task:** state the NEW figures only. Do NOT restate the superseded bound, superseded total span, or superseded candidate-spacing figure anywhere in these files' prose — Task 3's sweep gate asserts those numerals are absent from `RailRig.tsx` and `rail-rig-geometry.test.ts`. Refer to them by name ("the superseded footprint-derived overhang") instead of by numeral.
  </action>
  <verify>
    <automated>npx vitest run src/scene/rail-rig-geometry.test.ts src/collision</automated>
  </verify>
  <done>`TRACK_LENGTH` is 2.8 with ends at `RAIL_CENTER_X ± 1.4`; `TRACK_OVERHANG` is 0.1; `RailRig.tsx` has no footprint import and no `TRACK_END_MARGIN`; the rewritten geometry suite and the full collision suite pass.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the orphaned footprint module and sweep the remaining stale literals</name>
  <files>src/scene/robot-footprint.ts, src/scene/robot-footprint.test.ts, src/ui/manual-jog.test.ts, src/scene/camera-defaults.ts</files>
  <action>
    1. Confirm, with a repo-wide search over `src/` for both the module path and each of its three exported constant names, that Tasks 1-2 left NO remaining importer of `src/scene/robot-footprint.ts` other than its own measuring test. If the search is clean, delete BOTH `src/scene/robot-footprint.ts` and `src/scene/robot-footprint.test.ts` (the STL parser and forward-kinematics sweep live entirely in that test — the measurement approach is superseded in full, per the user's explicit instruction not to leave it orphaned). If the search finds a legitimate remaining consumer, do NOT delete: keep both files and name that consumer explicitly in the SUMMARY so the user can judge.

    2. In `src/ui/manual-jog.test.ts`, update the three stale bound expectations to the new range: the `railLimitsMillimetres` equality object, the two `clampRailMillimetres` out-of-range clamp results, and the millimetre/metre round-trip case (which should now round-trip 1300mm against 1.3m — rename that test accordingly). Change nothing else in that file: it sits next to the confirmed-working manual-input drift fix and only its rail-bound numbers are affected.

    3. In `src/scene/camera-defaults.ts`, update ONLY the stale prose in the doc comment that states how far the rail runs along X, so it names the new travel span and the new visible track span. Do NOT change any camera position, target, or field-of-view value — camera framing is out of scope and confirmed working.

    4. Do a final repo-wide search over `src/` for any other module still restating the superseded travel bound, the superseded total span, or the superseded candidate-spacing figure as a literal instead of deriving from `RAIL_TRAVEL` / `RAIL_CANDIDATE_SPACING_M`. Fix any hit by switching it to the derived constant. Ignore matches that are genuinely about something else (the robot's own height, unrelated joint-angle values, CSS/UI sizing) — the sweep is for RAIL geometry only.

    5. Run the full gate below. If any suite outside this plan's files fails, that is a real ripple effect — root-cause and fix it rather than adjusting the assertion, and record it in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx tsc -b &amp;&amp; npm run build &amp;&amp; ! grep -REq "1\.5|1500|0\.015" src/kinematics/rail.ts src/kinematics/rail.test.ts src/scene/RailRig.tsx src/scene/rail-rig-geometry.test.ts src/ui/manual-jog.test.ts</automated>
  </verify>
  <done>Full Vitest suite green, `tsc -b` clean, production build succeeds, the scoped literal sweep returns no hits, and the footprint module is either deleted (no importer) or kept with its remaining consumer named in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live browser verification of the capped travel range and trimmed track</name>
  <what-built>
    The rail's enforced travel range is now -1300mm to +1300mm (manual input and toolpath-resolved rail positions both clamp there), and the rendered rail track has been trimmed to a fixed 2.8 m — exactly -1400mm to +1400mm from the rail centre, with 100mm of visible track past each end-stop. The footprint-measurement approach from quick 260816-qym has been removed, not layered over. The mode-station offset was re-derived as a whole number of resolver grid steps (0.598 m) so the grid-alignment invariant now holds by construction under the smaller span.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app.

    1. **Enforced clamp (the core fix).** On the Dashboard, type a rail value well past the new limit (e.g. 2000) into the rail field and press Enter — it must clamp to 1300 and the robot must stop at that end. Repeat with a large negative value; it must clamp to -1300. Drag the rail slider hard to each end — same result. At BOTH extremes the robot must look normal, NOT like `Robot Problem.png`.

    2. **Trimmed track.** From several camera angles, confirm the rail track no longer runs on for a long stretch past the end-stops — it should end a short, deliberate distance beyond each end-stop block, not extend far into empty space.

    3. **Expected consequence to judge, not a bug.** At each travel extreme the carriage's mounting plate now slightly overhangs the visible track end (the plate is wider than the 100mm of track that remains). This falls straight out of the ±1400mm figure you specified. Say whether you want it left exactly as specified, or the visible track nudged out just enough to fully carry the plate.

    4. **No regressions in the confirmed-working areas.** Import/auto-load a g-code file in each of Printing and Milling, press Play and let the toolpath animate; scrub. Type an absolute value into a couple of joint fields (the off-by-one fix must still land exactly). Click Home / Reset Position mid-playback. Switch modes and confirm the carriage still moves to a visibly different station.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still looks wrong (screenshot welcome)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user typed input -> manual-jog commit | Human-entered rail/joint values cross into the kinematics/collision layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-s4e-01 | Tampering | `clampRailPosition` / `setManualRailPos` | low | mitigate | Rail input remains routed through `clampRailPosition`; the tightened bound is enforced at the single existing clamp, so no new bypass path is introduced (Task 1 changes only the constant, not the gate). |
| T-s4e-02 | Denial of Service | `resolveRailPosition` | low | accept | Candidate count is unchanged (201) and the loop body is explicitly not modified, so scan cost is identical; no new unbounded work. |
| T-s4e-03 | Tampering | dependency install | low | accept | This plan installs no packages — no npm/pip/cargo install task exists, so the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `npx vitest run` — full suite green (the pre-existing suite plus this plan's rewritten cases; the deleted footprint suite's cases are expected to disappear from the count).
- `npx tsc -b` — clean (catches any dangling import of the deleted module or the removed `TRACK_END_MARGIN` export).
- `npm run build` — production build succeeds (the pre-existing chunk-size warning is not a regression).
- The scoped literal sweep in Task 3's verify returns no hits.
- Task 4's live human checkpoint passes.
</verification>

<success_criteria>
- `RAIL_TRAVEL` is `{ min: -1.3, max: 1.3 }` and every rail consumer — manual input, sliders, toolpath resolution, end-stop meshes, collision AABBs — follows from that one constant.
- The rendered track spans exactly `RAIL_CENTER_X ± 1.4`.
- `MODE_RAIL_START_OFFSET_M` is an exact whole multiple of `RAIL_CANDIDATE_SPACING_M` by construction, asserted at 1e-9, not at one grid step.
- `src/scene/robot-footprint.ts` and its test are deleted (or kept with a named remaining consumer stated in the SUMMARY).
- No stale superseded bound / span / spacing literal remains in the rail geometry modules.
- Nothing in the manual-input drift fix, sliders, Home/Reset button, ModeBar layout, camera framing, or g-code playback pipeline changed behaviourally.
</success_criteria>

<output>
Create `.planning/quick/260816-s4e-cap-rail-travel-at-1300mm-trim-rail-visu/260816-s4e-SUMMARY.md` when done.

The SUMMARY MUST record, as first-class findings:
- The final derived value of `MODE_RAIL_START_OFFSET_M` and the step count it was derived from.
- Whether `src/scene/robot-footprint.ts` was deleted or kept, and if kept, exactly which module still consumes it.
- Every file touched by the stale-literal sweep in Task 3 step 4, including any found beyond the ones this plan already names.
- Any suite outside this plan's files that failed as a ripple effect, and how it was root-caused.
</output>
