---
phase: quick
plan: 260816-m6d
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kinematics/joint-clamp.ts
  - src/kinematics/joint-clamp.test.ts
  - src/kinematics/index.ts
  - src/ui/manual-jog.ts
  - src/ui/manual-jog.test.ts
  - src/store/cellStore.ts
  - src/store/cellStore.test.ts
  - src/store/uiShellStore.ts
  - src/scene/RobotPose.tsx
  - src/scene/CellScene.tsx
  - src/ui/tabs/DashboardPanel.tsx
  - src/ui/tabs/JobPanel.tsx
  - src/ui/tabs/tab-registry.ts
  - src/ui/tabs/tab-registry.test.ts
  - src/ui/shell/TabRail.tsx
  - src/ui/shell/TabPanel.tsx
  - src/ui/shell/PlaceholderPanel.tsx
  - src/ui/useModeJobSync.ts
  - src/ui/SampleSelect.tsx
  - src/ui/PlaybackControl.tsx
  - src/ui/ScrubControl.tsx
  - src/App.tsx
  - .planning/REQUIREMENTS.md
  # deleted by Task 3
  - src/ui/shell/ModeBar.tsx
  - src/ui/tabs/OperationsPanel.tsx
  - src/ui/tabs/SetupPanel.tsx
  - src/ui/tabs/VisionPanel.tsx
  - src/ui/tabs/CalibratePanel.tsx
  - src/ui/tabs/IoPanel.tsx
  - src/ui/tabs/OptimizationPanel.tsx
  - src/ui/useCellModeSampleSync.ts
autonomous: true

requirements:
  - SIM-01
  - SIM-05
  - DASH-01
  - DASH-03

# SCOPE CHANGE — read before executing.
# DASH-02 (live TCP Cartesian position and speed) is DESCOPED by explicit user
# direction ("Delete the TCP section of the dashboard entirely (robot has no
# tool, so it's irrelevant)"). Task 2 records that descope in
# .planning/REQUIREMENTS.md rather than silently dropping a tracked requirement.
descoped_requirements:
  - id: DASH-02
    reason: "User direction (quick 260816-m6d): the cell's flange is bare, so a TCP readout describes a tool that does not exist."

estimate:
  tokens: 160000
  raw_tokens: 80000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "User types a joint angle in degrees into any of the 6 Dashboard joint fields and the UR3e in the 3D scene moves to that commanded pose immediately, with no g-code loaded."
    - "User types 600 into the base-joint field and the field settles at that joint's real configured maximum (360) instead of rejecting the entry or leaving the robot at an illegal pose; the same clamping applies to every joint and to the rail."
    - "User types a rail position outside the rail's travel range and the carriage moves to the nearest end-stop position instead of off the rail."
    - "The Dashboard has no TCP section at all."
    - "The left tab rail shows exactly three tabs — Printing, Milling, Dashboard — and clicking Printing or Milling also switches the cell mode."
    - "No surface anywhere in the app announces a future roadmap phase for a placeholder readout."
    - "On first load the Printing tab already shows its bundled sample toolpath rendered in the scene; switching to Milling shows the mill sample without any manual picking."
    - "The Printing tab and the Milling tab each have their own upload button, and a file uploaded on one does not replace the other's job."
    - "The scrub/timeline bar is not visible until the user presses Play for the current job; after that it stays visible and draggable."
    - "The Play button is visibly larger than the other overlay controls and sits horizontally centred in the bottom-left control column."
    - "Selecting a bundled sample, playing it, and scrubbing it all still work exactly as they did before this plan (Phase 02/04 pipeline unbroken)."
  artifacts:
    - src/kinematics/joint-clamp.ts
    - src/ui/manual-jog.ts
    - src/ui/tabs/DashboardPanel.tsx
    - src/ui/tabs/JobPanel.tsx
    - src/ui/useModeJobSync.ts
  key_links:
    - "cellStore.manualJog -> RobotPose.tsx useFrame (manual pose must win over the trajectory sample, or typing a value does nothing visible)"
    - "cellStore.manualJog.railPos -> CellScene.tsx railPos selector -> RailRig railPos prop (or the carriage ignores the typed rail position)"
    - "uiShellStore.setActiveTab -> cellMode -> toolpathAnchorForMode/railStartXForMode (or the Printing/Milling tabs stop moving the work station)"
    - "cellStore.uploadedJobs[mode] -> loadJobForMode -> parseToolpath/compileTrajectory (or an uploaded file is stored but never rendered)"
    - "cellStore.playbackStarted -> App.tsx conditional ScrubControl mount (or the scrub bar never appears after Play)"
---

<objective>
Five user-directed interface changes, executed against the existing Phase 01-04
codebase without disturbing the g-code import -> toolpath playback pipeline:

1. Remove every future-phase placeholder announcement from the app.
2. Prune the tab rail down to Printing / Milling / Dashboard; delete the stub panels.
3. Rework the Dashboard into a real manual-control surface: 6 typed joint angles
   plus a typed rail position, every entry clamped (never rejected) to the real
   configured limits; the TCP section deleted outright.
4. Give the Printing and Milling tabs independent .gcode upload buttons, and have
   each tab arrive with its bundled sample already loaded.
5. Hide the scrub bar until Play is pressed, and make the Play button larger and
   horizontally centred.

Purpose: the interview build should present only surfaces that actually do
something, and should let a reviewer drive the robot directly without authoring
g-code first.

Output: a 3-tab shell, a manual-jog Dashboard, per-mode job loading with upload,
and a cleaned-up playback overlay.

**Limits are read, never invented.** Joint limits come from
`UR3E_JOINT_LIMITS` (`src/kinematics/ur3e-dh.ts`, cross-verified against
Universal Robots' `joint_limits.yaml`; note the elbow is deliberately narrower at
+/- pi and must NOT be normalised to match the others). Rail travel comes from
`RAIL_TRAVEL` / `clampRailPosition` (`src/kinematics/rail.ts`). No new limit
constant is introduced anywhere in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md

Current-state files the executor must read before editing (all already exist):
@src/kinematics/ur3e-dh.ts
@src/kinematics/rail.ts
@src/kinematics/index.ts
@src/store/cellStore.ts
@src/store/uiShellStore.ts
@src/scene/RobotPose.tsx
@src/scene/CellScene.tsx
@src/ui/tabs/tab-registry.ts
@src/ui/shell/TabPanel.tsx
@src/ui/shell/TabRail.tsx
@src/ui/shell/PlaceholderPanel.tsx
@src/ui/useCellModeSampleSync.ts
@src/ui/SampleSelect.tsx
@src/ui/ScrubControl.tsx
@src/ui/PlaybackControl.tsx
@src/App.tsx
@src/gcode/samples.ts

**House rules that constrain every task below** (from CLAUDE.md and
`src/store/cellStore.ts`'s own file header — both already enforced by the
existing code):
- Never write a per-animation-frame value into Zustand. Manual-jog values are
  human-typing cadence, so a normal reactive field is correct for them; the
  per-frame CONSUMER (`RobotPose.tsx`) still reads them via
  `useCellStore.getState()` inside `useFrame`, never a reactive selector.
- DOM controls under `src/ui/` dispatch store actions only; they never import
  from `src/scene/` and never touch the camera or robot directly.
- Vitest runs with `environment: 'node'` (`vite.config.ts`) — there is no DOM in
  tests. Every new test in this plan must therefore target pure functions or
  the Zustand stores, never a rendered component.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure clamp + numeric-input helpers for manual jog</name>
  <files>src/kinematics/joint-clamp.ts, src/kinematics/joint-clamp.test.ts, src/kinematics/index.ts, src/ui/manual-jog.ts, src/ui/manual-jog.test.ts</files>
  <read_first>src/kinematics/ur3e-dh.ts, src/kinematics/rail.ts, src/kinematics/index.ts, src/kinematics/rail.test.ts</read_first>
  <behavior>
    joint-clamp.test.ts:
    - `clampJointAngle(0, 10)` returns `2 * Math.PI` (base joint's real max from `UR3E_JOINT_LIMITS[0]`), not 10.
    - `clampJointAngle(0, -10)` returns `-2 * Math.PI`.
    - `clampJointAngle(2, 4)` returns `Math.PI` — the elbow's narrower limit is honoured, proving the table is read per index rather than a single shared range.
    - `clampJointAngle(2, -4)` returns `-Math.PI`.
    - An in-range value is returned unchanged for every one of the 6 indices.
    - `clampJointAngle(i, NaN)` and `clampJointAngle(i, Infinity)` return a finite in-range number (choose the nearest limit / 0 for NaN) — never NaN, since a NaN reaching `setJointValue` silently corrupts the rendered pose.
    - An out-of-bounds joint index throws (or returns the input unchanged — pick one and assert it) rather than reading `undefined.min`.
    - `clampJointAngles([...])` clamps all six positionally and returns a new tuple.
    - Structural guard: for every index, the clamped output satisfies the existing `isWithinJointLimits` from `forward-kinematics.ts`.

    manual-jog.test.ts:
    - `parseNumericInput('')`, `('-')`, `('.')`, `('-.')`, `('abc')`, `('1e')` all return `null` (in-progress typing must not be committed).
    - `parseNumericInput('-90')` returns `-90`; `parseNumericInput('600')` returns `600`; `parseNumericInput(' 12.5 ')` returns `12.5`.
    - `parseNumericInput('Infinity')` and `parseNumericInput('NaN')` return `null`.
    - `toRadians(180)` is `Math.PI` and `toDegrees(Math.PI)` is `180` (round-trip within 1e-9).
    - `jointLimitsDegrees(0)` returns `{ min: -360, max: 360 }` and `jointLimitsDegrees(2)` returns `{ min: -180, max: 180 }`, both DERIVED from `UR3E_JOINT_LIMITS` rather than restated as literals.
    - `railLimitsMillimetres()` returns `{ min: -1500, max: 1500 }`, derived from `RAIL_TRAVEL`.
    - `clampRailMillimetres(9999)` returns `1500`; `clampRailMillimetres(-9999)` returns `-1500`; `clampRailMillimetres(250)` returns `250`.
    - `millimetresToMetres(1500)` is `1.5` and `metresToMillimetres(1.5)` is `1500`.
  </behavior>
  <action>
Create `src/kinematics/joint-clamp.ts` exporting `clampJointAngle(jointIndex, radians)` and `clampJointAngles(joints)`. Both read `UR3E_JOINT_LIMITS` from `./ur3e-dh` by index — do not restate any limit value as a literal in this file, and do not normalise the elbow's narrower range (that asymmetry is a real mechanical constraint documented in `ur3e-dh.ts`). Mirror `rail.ts`'s `clampRailPosition` in shape and doc-comment style: this is the joint-space counterpart of that function, and the two are the only clamping authorities in the project. Guard non-finite input explicitly so a NaN can never reach `setJointValue`.

Re-export `clampJointAngle` and `clampJointAngles` from the `src/kinematics/index.ts` barrel, in the same block that already exports `UR3E_JOINT_LIMITS` — the barrel is the single import surface scene and UI code consume.

Create `src/ui/manual-jog.ts` — the degrees/millimetres presentation layer, kept OUT of `src/kinematics/` because the kinematics modules are radians-and-metres-native and must stay that way. Export: `parseNumericInput(text): number | null` (returns null for empty/partial/non-finite text so a controlled input can hold an in-progress string like `-` without committing it), `toRadians`/`toDegrees`, `millimetresToMetres`/`metresToMillimetres`, `jointLimitsDegrees(jointIndex)` (converted from `UR3E_JOINT_LIMITS` via the barrel), `railLimitsMillimetres()` (converted from `RAIL_TRAVEL`), and `clampRailMillimetres(mm)` (converts to metres, routes through the existing `clampRailPosition`, converts back). Also export `formatDegrees(radians)` / `formatMillimetres(metres)` returning fixed-precision display strings (1 decimal place) so the panel in Task 2 has one formatting authority.

Write both test files first and watch them fail before implementing.
  </action>
  <verify>
    <automated>cd "C:/Users/munte/Claude Projects/Interface" &amp;&amp; npx vitest run src/kinematics/joint-clamp.test.ts src/ui/manual-jog.test.ts &amp;&amp; npx tsc -b</automated>
  </verify>
  <done>Both new test files pass, `tsc -b` is clean, and neither new module contains a hardcoded angle or travel bound — every limit is derived from `UR3E_JOINT_LIMITS` / `RAIL_TRAVEL`.</done>
  <reversibility rating="reversible">Two new pure modules plus two barrel exports; nothing existing changes behaviour.</reversibility>
</task>

<task type="tracer">
  <name>Task 2: Manual jog end-to-end — typed joint/rail values drive the robot; TCP section deleted</name>
  <files>src/store/cellStore.ts, src/scene/RobotPose.tsx, src/scene/CellScene.tsx, src/ui/tabs/DashboardPanel.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>src/store/cellStore.ts, src/scene/RobotPose.tsx, src/scene/CellScene.tsx, src/ui/tabs/DashboardPanel.tsx, src/ui/shell/PlaceholderPanel.tsx, src/kinematics/ur3e-dh.ts</read_first>
  <action>
This is the vertical slice: store field -> per-frame consumer -> scene prop -> UI control. Wire the whole path before adding anything else.

**cellStore.ts** — add one reactive field and its actions:
- `manualJog: { joints: JointAngles; railPos: number } | null`, initial `null`. Null means "the compiled trajectory drives the robot", which is today's only behaviour, so nothing changes until the user types something.
- `setManualJointAngle(jointIndex: number, radians: number)`: seeds `manualJog` from `UR3E_PARKED_POSE` and the current rail position (`get().trajectory?.railPos ?? get().lastRailPos`) when it is currently null, then writes `clampJointAngle(jointIndex, radians)` into the indexed slot of a NEW tuple (never mutate the existing one — Zustand subscribers compare by reference).
- `setManualRailPos(metres: number)`: same seeding rule, then `clampRailPosition(metres)`.
- `clearManualJog()`: sets `manualJog` back to `null`, but only calls `set()` when it is not already null, so a repeated call cannot churn subscribers.
- Call `clearManualJog()`'s effect from inside `play()` (pressing Play means "run the toolpath", which must take the robot back off manual command) and from every branch of the sample-loading action that already resets `scrubFraction`. Do NOT put it inside `setScrubFraction` — the playback clock calls that setter on its own throttled sync, and this file's header already documents why a setter with side effects there is a trap (Pitfall 4). The scrub-drag case is handled at the UI level in Task 5, exactly as the existing drag-pauses-playback rule already is.
- Add a doc comment stating why this field is allowed in a store that forbids per-frame values: it changes at typing cadence, and its per-frame consumer reads it non-reactively.

**RobotPose.tsx** — inside the existing `useFrame`, destructure `manualJog` from the same `useCellStore.getState()` call that already reads `trajectory` and `livePlayback`. When `manualJog` is non-null, feed `toUrdfJointAngles(manualJog.joints)` into `setJointValue` for each `UR3E_JOINT_NAMES` entry and return early — the manual command wins over the trajectory sample. Everything below stays exactly as it is. Keep routing through `toUrdfJointAngles`: the URDF's `base_link_inertia` frame divergence applies to a manually commanded pose just as much as a solved one, and skipping it would render the arm 180 degrees around the vertical axis from what the user typed.

**CellScene.tsx** — extend the existing rail selector to `state.manualJog?.railPos ?? state.trajectory?.railPos ?? state.lastRailPos`. Manual command first, then the compiled trajectory, then the CR-01 last-known-good fallback. Do not touch mount order — `cell-scene-order.test.ts` guards it.

**DashboardPanel.tsx** — rewrite. Keep `PanelShell` / `PanelSection` / `ReadoutRow` from `PlaceholderPanel.tsx`; drop the future-phase footer import entirely.
- Add a local `NumberField` component in this file (label, controlled `<input type="number">`, a `min…max` hint line, and a unit suffix). It holds the in-progress text in local `useState` and commits through `parseNumericInput`: on change, update the draft and — when the draft parses — dispatch the store action; on blur, rewrite the draft from the (clamped) store value so an out-of-range entry visibly snaps to the limit instead of lingering as typed text. Style it with the same `--ui-*` / `--space-*` / `--text-*` custom properties the sibling primitives use; introduce no new colour.
- Section "Joint angles — manual control": six `NumberField`s, labels Base / Shoulder / Elbow / Wrist 1 / Wrist 2 / Wrist 3, values in DEGREES, each showing its own `jointLimitsDegrees(i)` range as the hint. Displayed value is `formatDegrees` of `manualJog?.joints[i] ?? UR3E_PARKED_POSE[i]`, so the numbers on screen are always exactly the numbers that will be commanded. Dispatch `setManualJointAngle(i, toRadians(parsed))`.
- Section "Rail — 7th axis": one `NumberField` in millimetres bounded by `railLimitsMillimetres()`, dispatching `setManualRailPos(millimetresToMetres(parsed))`; plus two `ReadoutRow`s for remaining travel in each direction, computed with the existing `railRemainingTravel` from the commanded rail position and formatted in millimetres.
- A "Return to toolpath" button, rendered only while `manualJog` is non-null, dispatching `clearManualJog()`, with one muted line stating that manual command is currently overriding playback.
- There must be NO TCP section and no Cartesian/speed readout of any kind — the flange is bare, so such a readout would describe a tool that does not exist.

**.planning/REQUIREMENTS.md** — DASH-02 is the tracked requirement the deleted section served. Mark its status row `Descoped` and add the reason (user direction, quick 260816-m6d, bare flange) next to it. Leave DASH-01 and DASH-03 rows alone.

Also extend `src/store/cellStore.test.ts` with a `manualJog` describe block: an out-of-range `setManualJointAngle` lands on the joint's real limit; `setManualRailPos(99)` lands on `RAIL_TRAVEL.max`; the first call seeds the other five joints from `UR3E_PARKED_POSE`; `play()` returns `manualJog` to null; each setter produces a new tuple reference.
  </action>
  <verify>
    <automated>cd "C:/Users/munte/Claude Projects/Interface" &amp;&amp; npx vitest run src/store/cellStore.test.ts src/scene/cell-scene-order.test.ts &amp;&amp; npx tsc -b &amp;&amp; ! grep -rniE "\bTCP\b" src/ui/tabs/DashboardPanel.tsx</automated>
  </verify>
  <done>Every pre-existing `cellStore.test.ts` test still passes unchanged (the regression gate on the Phase 02/04 pipeline), the new manual-jog tests pass, `tsc -b` is clean, and `DashboardPanel.tsx` contains no TCP reference. Manually: typing 600 in the base field settles at 360 and the arm swings there with no g-code loaded.</done>
  <reversibility rating="reversible">Additive store field with a null default; every existing code path is unchanged when `manualJog` is null.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Prune the tab rail to Printing / Milling / Dashboard and delete all future-phase placeholder copy</name>
  <files>src/ui/tabs/tab-registry.ts, src/ui/tabs/tab-registry.test.ts, src/ui/shell/TabRail.tsx, src/ui/shell/TabPanel.tsx, src/ui/shell/PlaceholderPanel.tsx, src/store/uiShellStore.ts, src/App.tsx</files>
  <read_first>src/ui/tabs/tab-registry.ts, src/ui/tabs/tab-registry.test.ts, src/ui/shell/TabRail.tsx, src/ui/shell/TabPanel.tsx, src/ui/shell/PlaceholderPanel.tsx, src/ui/shell/ModeBar.tsx, src/store/uiShellStore.ts, src/cell-mode.ts</read_first>
  <action>
**Delete these files outright** (each is a stub with no real behaviour, and each carries future-phase placeholder copy): `src/ui/tabs/OperationsPanel.tsx`, `src/ui/tabs/SetupPanel.tsx`, `src/ui/tabs/VisionPanel.tsx`, `src/ui/tabs/CalibratePanel.tsx`, `src/ui/tabs/IoPanel.tsx`, `src/ui/tabs/OptimizationPanel.tsx`, and `src/ui/shell/ModeBar.tsx`.

ModeBar goes because the tab rail absorbs its job: with Printing and Milling promoted to tabs, a second segmented mode switch elsewhere on screen would be a duplicate control that can visibly disagree with the rail. Its one piece of real information — the mounted-tool label — moves into the per-mode panel in Task 4.

**tab-registry.ts** — `TabId` becomes `CellMode | 'dashboard'` (import `CellMode` from `../../cell-mode`, the dependency-free module that exists for exactly this kind of reach). `TAB_DEFS` becomes three entries: printing, milling, dashboard, each with `id` and `label` only. Delete the `phase` and `blurb` fields from `TabDef` — both existed solely to render placeholder copy. `DEFAULT_TAB_ID` stays `'dashboard'`. Add a pure `cellModeForTab(id: TabId): CellMode | null` returning the id itself for the two mode tabs and `null` for dashboard — this is the one place the tab/mode correspondence is written down.

**uiShellStore.ts** — `setActiveTab(id)` now also syncs the cell mode: when `cellModeForTab(id)` is non-null it sets `cellMode` in the same `set()` call, otherwise it sets `activeTab` alone (opening Dashboard must not change which mode the cell is configured for). Keep `setCellMode` as the store's own mode setter but note in its doc comment that the tab rail is now its only UI-facing dispatcher. Update the store's header comment: the tab id and the cell mode are now deliberately coupled for two of the three tabs, and that coupling lives here rather than in the rail component.

**TabRail.tsx** — reduce `TAB_ICONS` to the three ids. Use `Printer` for printing and `Drill` (or `Wrench` if `Drill` is absent from the installed lucide-react version — verify against `node_modules/lucide-react` before choosing) for milling; keep `LayoutDashboard` for dashboard. The `Record<TabId, LucideIcon>` typing already fails the build if an id has no icon — keep it. No other change: the click handler still dispatches `setActiveTab` and nothing else.

**TabPanel.tsx** — `PANELS` shrinks to the three ids. Dashboard maps to `DashboardPanel`; printing and milling map to thin module-scope wrappers over the shared job panel Task 4 creates. To keep this task independently green, define them here now as `const PrintingPanel = () => <JobPanel mode="printing" />` and the milling equivalent, and create `src/ui/tabs/JobPanel.tsx` in this task as a minimal panel that renders the mode's title and its mounted-tool line via `PanelShell`/`PanelSection` — Task 4 fills in the upload control and job readout.

**PlaceholderPanel.tsx** — delete the `PhaseNote` export and the `phaseNoteStyle` const. Keep `PanelShell`, `PanelSection` and `ReadoutRow`; they are real layout primitives the surviving panels still compose from. Update the file header, which currently describes seven panels.

**App.tsx** — remove the `ModeBar` import and its element from the tree.

**tab-registry.test.ts** — rewrite for the new registry: exactly 3 entries; the id set equals `['printing','milling','dashboard']`; no duplicate ids; every entry has a non-empty label; `DEFAULT_TAB_ID` is one of the ids; the existing `PANELS`-parity test survives verbatim (every registry id resolves to a defined panel and `PANELS` has no orphan key). Add: `cellModeForTab('printing') === 'printing'`, `cellModeForTab('milling') === 'milling'`, `cellModeForTab('dashboard') === null`. Delete the phase-range assertion along with the field it tested.
  </action>
  <verify>
    <automated>cd "C:/Users/munte/Claude Projects/Interface" &amp;&amp; npx vitest run src/ui/tabs/tab-registry.test.ts &amp;&amp; npx tsc -b &amp;&amp; npm run build &amp;&amp; ! grep -rn "Arrives in Phase" src/</automated>
  </verify>
  <done>`tsc -b` and the production build both succeed with the seven deleted files gone (proving nothing still imports them), the registry test passes against exactly 3 tabs, and the recursive grep over `src/` finds no future-phase announcement copy.</done>
  <reversibility rating="costly">Seven files are deleted. Recoverable from git history, but the stub panels are not re-derivable from the remaining code.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Per-mode g-code upload, with each mode's bundled sample loaded by default</name>
  <files>src/store/cellStore.ts, src/ui/tabs/JobPanel.tsx, src/ui/useModeJobSync.ts, src/ui/SampleSelect.tsx, src/App.tsx</files>
  <read_first>src/store/cellStore.ts, src/store/cellStore.test.ts, src/gcode/samples.ts, src/gcode/parseToolpath.ts, src/gcode/toolpath-anchor.ts, src/ui/useCellModeSampleSync.ts, src/ui/SampleSelect.tsx, src/ui/tabs/JobPanel.tsx</read_first>
  <action>
**cellStore.ts — refactor `selectSample` into one shared loader, then add uploads.**
Extract the fetch/parse/compile/commit body into a single internal async action that takes a job source discriminated union: `{ kind: 'sample'; sampleId }` or `{ kind: 'upload'; mode; fileName; text }`. Preserve every existing behaviour exactly as documented in the current function — the monotonic `selectSampleRequestId` stale-response guard, resolving the anchor via `toolpathAnchorForMode` BEFORE the first await, compiling inside the same guard before any further await, writing `lastRailPos` only on the success branch, resetting `scrubFraction`/`livePlayback.fraction`/`isPlaying` on every branch, and recording `lastSelectSampleOrigin` on every branch. For an upload the only difference is where the g-code text comes from (the in-memory string instead of `fetch`) and that the anchor is resolved from the source's own `mode` rather than the live store read. `selectSample(sampleId, origin = 'manual')` becomes a one-line delegation, so its existing signature and every existing call site and test stay untouched.

Then add:
- `uploadedJobs: Record<CellMode, { fileName: string; text: string } | null>`, initial `{ printing: null, milling: null }`. Per-mode, so a file loaded on one tab can never replace the other tab's job.
- `MAX_UPLOAD_BYTES` (5 MiB) exported from this module, and `UPLOADED_JOB_ID` (a sentinel `selectedSampleId` value that matches no entry in `GCODE_SAMPLES`).
- `loadUploadedGcode(mode, fileName, text)`: rejects text longer than `MAX_UPLOAD_BYTES` by setting `toolpathLoadStatus: 'error'` and leaving the previous job's `uploadedJobs` entry untouched; otherwise records the entry and delegates to the shared loader with `selectedSampleId` set to the sentinel.
- `clearUploadedJob(mode)`: drops that mode's entry and reloads its bundled sample.
- `loadJobForMode(mode, origin)`: dispatches the mode's uploaded job when it has one, otherwise `selectSample(firstSampleIdForMode(mode), origin)`. When that helper returns null (a mode with no bundled sample), set an idle/empty state and return rather than dispatching an empty id.

**src/ui/useModeJobSync.ts** — new file replacing `src/ui/useCellModeSampleSync.ts` (delete the old one and its import in `App.tsx`; mount the new hook in the same place, for the same reason its predecessor documents: this is the one shell-level place the UI-chrome store and the simulation store are deliberately crossed). Behaviour:
- On the initial mount, dispatch `loadJobForMode(cellMode, 'mode-sync')`. This is the deliberate reversal of the predecessor's "must never auto-load on startup" rule — the user now wants each mode's sample already on screen. Use the `'mode-sync'` origin, not `'manual'`, so `ToolpathCameraFit.tsx` keeps the wide whole-cell default framing on startup instead of immediately zooming to the toolpath's bounding box.
- On any later `cellMode` change, dispatch `loadJobForMode(newMode, 'mode-sync')` unconditionally. The predecessor's `sampleMatchesMode` guard is dropped on purpose: with per-mode uploads, "the right job for this mode" is no longer answerable from the sample table alone, and a redundant reload is one cheap parse+compile.
- Keep reading the simulation store via `getState()` inside the effect so the effect's only dependency stays `cellMode`.

**JobPanel.tsx** — flesh out the Task 3 stub. Props: `{ mode: CellMode }`. Renders:
- the mounted-tool line inherited from the deleted ModeBar (print head / mill spindle);
- an "Upload .gcode" `Button` wired to a visually hidden `<input type="file" accept=".gcode,.nc,.txt,text/plain">` via a ref, whose change handler reads `file.text()` and dispatches `loadUploadedGcode(mode, file.name, text)`, then clears the input's value so re-selecting the same filename fires again;
- a readout of the currently loaded job for this mode — the uploaded filename when `uploadedJobs[mode]` is set, otherwise the bundled sample's label;
- a "Use bundled sample" button, shown only while an upload is active, dispatching `clearUploadedJob(mode)`;
- a `role="status"` line when the last load errored, stating the size cap when that was the cause.
This component dispatches store actions only — no scene or parser import.

**SampleSelect.tsx** — when `uploadedJobs[cellMode]` is set, render one extra `<option value={UPLOADED_JOB_ID} disabled>` labelled with the filename, so the dropdown displays the truly-loaded job instead of falling back to its "select a sample" placeholder. `disabled` keeps it displayable-but-unselectable, the same trick the existing empty placeholder option already uses, which means `onChange` still only ever fires with a real sample id. Leave the unit note and the skipped-command / frozen-trajectory status lines exactly as they are — they are this app's transparency guarantee and must keep rendering for uploaded files too.

Extend `src/store/cellStore.test.ts`: an upload for printing leaves `uploadedJobs.milling` null; `loadJobForMode` prefers an uploaded job over the bundled sample; over-cap text lands on `toolpathLoadStatus: 'error'` without recording the entry; a sample selection after an upload still resolves normally.
  </action>
  <verify>
    <automated>cd "C:/Users/munte/Claude Projects/Interface" &amp;&amp; npx vitest run src/store/cellStore.test.ts &amp;&amp; npx tsc -b &amp;&amp; npm run build</automated>
  </verify>
  <done>All pre-existing `cellStore.test.ts` tests pass unchanged after the loader refactor (the hard gate that the Phase 02/04 import pipeline is intact), the new upload tests pass, and the build is clean. Manually: the app opens on the Printing tab with the print sample already drawn; switching to Milling draws the mill sample; uploading a file on Milling leaves Printing's job untouched.</done>
  <reversibility rating="costly">`selectSample`'s body is restructured. Its external signature and every existing test are unchanged, so the blast radius is contained to this one function.</reversibility>
</task>

<task type="auto">
  <name>Task 5: Scrub bar hidden until Play; larger, centred Play button</name>
  <files>src/store/cellStore.ts, src/App.tsx, src/ui/PlaybackControl.tsx, src/ui/ScrubControl.tsx</files>
  <read_first>src/store/cellStore.ts, src/App.tsx, src/ui/PlaybackControl.tsx, src/ui/ScrubControl.tsx, src/components/ui/button.tsx</read_first>
  <action>
**cellStore.ts** — add `playbackStarted: boolean`, initial `false`. Set it `true` inside `play()`. Reset it to `false` on every branch of the shared job loader from Task 4 that already resets `scrubFraction`, so loading a new job hides the bar again and the "press Play to unlock the timeline" affordance repeats per job. Do not reset it in `pause()` — the whole point is that the bar stays draggable once playback has begun.

**App.tsx** — read `playbackStarted` with a single-field selector and render `<ScrubControl />` only when it is true. Keep it in the same overlay column, in the same position beneath the Play button, so its appearance reads as the timeline unlocking rather than the layout reflowing somewhere else.

**PlaybackControl.tsx** — keep the component's existing contract (reads `isPlaying`/`trajectory`, dispatches `play`/`pause`, `variant="secondary"`, never the reserved accent tone). Two changes: (a) make the control visibly larger than the neighbouring overlay controls — start from `size="icon-lg"` and widen it with a `className` (a ~56px round target with a ~28px glyph reads correctly against the 240px-wide column); (b) wrap it in a `display: flex; justify-content: center; width: 100%` div so it sits centred across the overlay column's width instead of hugging the left edge. Keep the `aria-label` toggle and the disabled-when-no-trajectory guard.

**ScrubControl.tsx** — add one line to the change handler: dispatch `clearManualJog()` alongside the existing pause-on-drag, so dragging the timeline hands the robot back from manual command to the toolpath. This belongs here, not in `setScrubFraction`, for the reason the existing comment in this same handler already gives about the playback clock's throttled sync. Everything else in this file is unchanged. The `disabled`-state explanation line can go, since the control is no longer mounted before a job is playable — but keep the frozen/skipped transparency notes that live in `SampleSelect.tsx` untouched.

Extend `src/store/cellStore.test.ts`: `playbackStarted` starts false; `play()` sets it true; `pause()` leaves it true; loading a job returns it to false.
  </action>
  <verify>
    <automated>cd "C:/Users/munte/Claude Projects/Interface" &amp;&amp; npx vitest run &amp;&amp; npx tsc -b &amp;&amp; npm run build</automated>
  </verify>
  <done>The whole suite passes, the build is clean, and manually: on load the timeline is absent, pressing the (now large, centred) Play button reveals it, pausing keeps it, and switching tabs/loading a new job hides it again.</done>
  <reversibility rating="reversible">One boolean field plus a conditional mount and styling changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file -> `loadUploadedGcode` -> `parseToolpath` | User-supplied .gcode text of arbitrary size and content crosses into the parser and then into WebGL geometry. First untrusted-content boundary in this app — every prior toolpath came from a bundled, developer-authored file. |
| Dashboard numeric input -> `setManualJointAngle`/`setManualRailPos` -> `setJointValue` | Free-text numeric entry becomes a commanded robot pose with no g-code, IK solve, or trajectory compile in between. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-M6D-01 | Denial of Service | `loadUploadedGcode` (Task 4) | medium | mitigate | Reject text over `MAX_UPLOAD_BYTES` (5 MiB) before parsing; the parser's existing `MAX_TOOLPATH_SEGMENTS` (5000) cap already bounds geometry regardless of file size. Both limits surface a visible status line rather than failing silently. |
| T-M6D-02 | Tampering | manual jog inputs (Tasks 1-2) | medium | mitigate | Every typed value routes through `clampJointAngle` / `clampRailPosition` before reaching `setJointValue` or the rail prop; `parseNumericInput` returns null for NaN/Infinity/partial text so a non-finite value can never be committed, and `clampJointAngle` guards non-finite input a second time at the kinematics boundary. |
| T-M6D-03 | Information Disclosure | uploaded file contents | low | accept | The file is read entirely client-side via `File.text()` and held in memory only; this plan adds no network egress, no persistence, and no logging of file contents. |
| T-M6D-04 | Tampering | `selectSample` loader refactor (Task 4) | high | mitigate | The monotonic `selectSampleRequestId` stale-response guard and the compile-before-any-further-await ordering are preserved verbatim in the shared loader; the unchanged pre-existing `cellStore.test.ts` suite is the regression gate that proves it. |
| T-M6D-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no new packages. Every import used is already in `package.json`; no package-legitimacy audit is required. |
</threat_model>

<verification>
Run from the repo root after all five tasks:

1. `npx vitest run` — whole suite green, including every pre-existing test untouched by this plan.
2. `npx tsc -b` — clean (this is the real type gate; bare `tsc --noEmit` checks nothing here, per STATE.md).
3. `npm run build` — production build succeeds with seven deleted files gone.
4. `! grep -rn "Arrives in Phase" src/` — no future-phase announcement copy survives.
5. `npm run dev`, then walk the five user requirements in order:
   - Tab rail shows exactly Printing, Milling, Dashboard.
   - Printing tab opens with its sample toolpath already drawn; Milling likewise; uploading on one leaves the other's job intact.
   - Dashboard: typing 600 into Base settles at 360 and the arm moves there; typing -9999 into the rail field parks the carriage at the negative end-stop; there is no TCP section.
   - No timeline is visible until Play is pressed; the Play button is large and centred; after pressing Play the timeline appears and drags.
   - Dragging the timeline while a manual pose is commanded hands the robot back to the toolpath.
</verification>

<success_criteria>
- All five user requirements are observably satisfied in the running app.
- Every `must_haves.truths` entry is demonstrable.
- The Phase 02/04 pipeline (select sample -> parse -> compile -> play -> scrub) behaves exactly as before, proven by the unchanged pre-existing `cellStore.test.ts` suite passing.
- No new limit constant exists anywhere: joint limits still come only from `UR3E_JOINT_LIMITS`, rail travel only from `RAIL_TRAVEL`/`clampRailPosition`.
- DASH-02's descope is recorded in `.planning/REQUIREMENTS.md`, not silently dropped.
</success_criteria>

<output>
Create `.planning/quick/260816-m6d-dashboard-rework-manual-joint-rail-contr/260816-m6d-SUMMARY.md` when done.
</output>
