---
phase: quick
plan: 260815-3cn
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/App.tsx
  - src/scene/CellScene.tsx
  - src/ui/SampleSelect.tsx
  - src/ui/ScrubControl.tsx
  - src/ui/SceneStatusOverlay.tsx
  - src/store/uiShellStore.ts
  - src/ui/tabs/tab-registry.ts
  - src/ui/tabs/tab-registry.test.ts
  - src/ui/shell/TabRail.tsx
  - src/ui/shell/ModeBar.tsx
  - src/ui/shell/TabPanel.tsx
  - src/ui/shell/PlaceholderPanel.tsx
  - src/ui/tabs/DashboardPanel.tsx
  - src/ui/tabs/OperationsPanel.tsx
  - src/ui/tabs/SetupPanel.tsx
  - src/ui/tabs/VisionPanel.tsx
  - src/ui/tabs/CalibratePanel.tsx
  - src/ui/tabs/IoPanel.tsx
  - src/ui/tabs/OptimizationPanel.tsx
autonomous: true

# SCAFFOLD-ONLY. These IDs are the roadmap requirements whose *UI surface* gets a
# placeholder here. NONE of them are satisfied by this plan — no data is wired, no
# behaviour is implemented. Phases 5-8 still own their real delivery.
requirements_status: scaffold-only
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - SIM-03
  - SIM-06
  - SIM-07
  - TOOL-01
  - TOOL-02
  - SETUP-01
  - VISION-01
  - CALIB-01
  - CALIB-02
  - IO-01
  - OPT-01
  - OPT-02

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User sees a left-side vertical tab rail with 7 tabs (Dashboard, Operations, Setup, Vision, Calibrate, I/O, Optimization) and clicking a tab swaps the visible panel."
    - "User sees a Printing/Milling mode switcher plus a tool-changer 'mounted tool' chip that follows the selected mode (Phase 7 surface, visual only)."
    - "Every panel carries an explicit 'arrives in Phase N' note, so no placeholder can be mistaken for a live readout."
    - "The whole interface renders in a red + dark-grey palette, with the DOM chrome and the WebGL canvas clear color matching rather than clashing."
    - "Phase 1-3 behaviour is unchanged: the 3D scene renders, the sample select loads a toolpath, and the scrub control re-poses the robot."
  artifacts:
    - src/index.css
    - src/ui/tabs/tab-registry.ts
    - src/ui/tabs/tab-registry.test.ts
    - src/store/uiShellStore.ts
    - src/ui/shell/TabRail.tsx
    - src/ui/shell/ModeBar.tsx
    - src/ui/shell/TabPanel.tsx
    - src/ui/shell/PlaceholderPanel.tsx
    - src/ui/tabs/DashboardPanel.tsx
    - src/ui/tabs/OperationsPanel.tsx
    - src/ui/tabs/SetupPanel.tsx
    - src/ui/tabs/VisionPanel.tsx
    - src/ui/tabs/CalibratePanel.tsx
    - src/ui/tabs/IoPanel.tsx
    - src/ui/tabs/OptimizationPanel.tsx
  key_links:
    - "tab-registry.ts is the single source of truth for BOTH the rail's buttons and TabPanel's panel map — a tab cannot appear in the rail without a panel component behind it (asserted by tab-registry.test.ts)."
    - "uiShellStore.activeTab selects which panel TabPanel renders; uiShellStore.cellMode selects the ModeBar's mounted-tool chip."
    - "--ui-dominant feeds BOTH the index.css page background and CellScene's gl.setClearColor — the DOM/canvas tone-sync invariant CellScene.tsx already documents."
    - "--shell-rail-width + --shell-panel-width feed BOTH the shell's own fixed geometry and App.tsx's left-overlay offset, so the pre-existing SampleSelect/ScrubControl stack cannot end up hidden behind the new panel."
---

<objective>
Stand up static UI shell scaffolding for the elements Phases 5-8 will eventually deliver — a left tab rail with 7 tabs, a Printing/Milling mode bar with a tool-changer chip, and placeholder panels for Dashboard, Operations, Setup, Vision, Calibrate, I/O and Optimization — and re-theme the app from its current light palette to Red + Dark Grey.

Phase 4 (Playback Engine) is deliberately skipped for now. Because playback does not exist, **nothing in this plan wires real data**. Every readout is a static placeholder that names the phase which will fill it.

Purpose: make the full tab structure and layout demonstrable now, under interview time pressure, without pretending any of it is functional — and get the palette change landed once, before 7 new panels are written against the old tokens.

Output: a re-themed token layer, a tab-registry-driven shell, and 7 placeholder panels. All existing Phase 1-3 behaviour untouched.

Scope guard: no new npm dependencies. The shell is hand-built from native controls plus the one already-installed shadcn component (`src/components/ui/button.tsx`), matching the convention `SampleSelect.tsx` and `ScrubControl.tsx` already document (the installed shadcn CLI 4.x diverges from the 3.8.4 preset flow this project is pinned to, so pulling registry components would reopen a resolved decision).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.claude/CLAUDE.md

@src/index.css
@src/App.tsx
@src/ui/SampleSelect.tsx
@src/ui/ScrubControl.tsx
@src/ui/SceneStatusOverlay.tsx
@src/components/ui/button.tsx
@src/store/cellStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme the token layer to Red + Dark Grey and de-collide the UI-SPEC role tokens</name>

  <files>src/index.css, src/ui/SampleSelect.tsx, src/ui/ScrubControl.tsx, src/ui/SceneStatusOverlay.tsx, src/scene/CellScene.tsx</files>

  <read_first>
Read `src/index.css` in full before editing. There is a live naming collision that MUST be understood first:

- The first `:root` block (~lines 36-40) declares four UI-SPEC role properties under Tailwind's reserved `--color-` namespace.
- The later `@theme inline` block (~lines 84-90) re-declares three of those same names as aliases of the shadcn `--secondary` / `--accent` / `--destructive` variables.
- `@theme inline` wins (it is emitted later). So today `var(--color-secondary)` — which `SampleSelect.tsx` and `ScrubControl.tsx` use for **text color** — actually resolves to `oklch(0.97 0 0)`, a near-white, not the `#e4e7eb` the UI-SPEC comment claims. It only looked correct because both happened to be light greys on a light background.

This is the whole reason the rename below is mandatory: the moment the shadcn `--secondary` variable is flipped to a dark surface tone, every one of those text-color usages would become dark-grey-on-dark-grey and vanish.
  </read_first>

  <action>
Step 1 — Rename the UI-SPEC role tokens out of Tailwind's reserved namespace. In the first `:root` block of `src/index.css`, replace the four role properties (dominant / secondary / accent / destructive, currently prefixed with Tailwind's reserved color namespace) with a `--ui-` prefixed set, and extend it with the surface/foreground roles a dark theme needs:

- `--ui-dominant: #1C1E21` — page background and WebGL canvas clear color (60%)
- `--ui-surface: #26292E` — panel and card surfaces (30%)
- `--ui-surface-raised: #30343A` — selected/hovered tab, input backgrounds
- `--ui-border: #3C4149` — hairlines, panel dividers, input borders
- `--ui-fg: #F2F3F5` — primary text
- `--ui-fg-muted: #A2A8B2` — labels, secondary/passive text, placeholder dashes
- `--ui-accent: #E11D2E` — the red; reserved for the active tab indicator, the primary CTA, and the nav-cube hover state (10%)
- `--ui-accent-fg: #FFFFFF` — text on accent fill
- `--ui-destructive: #F97066` — kept distinct from the accent red so a future destructive action is still legible against it; unused this plan

Keep the existing `--space-*`, `--text-*`, `--leading-*` and `--weight-*` tokens exactly as they are — they do not collide with anything and later panels depend on them. Keep the UI-SPEC comment block above them, updated to describe the new palette.

Step 2 — Add the shell geometry tokens to the same `:root` block, so the shell's own layout and `App.tsx`'s overlay offset read one number:

- `--shell-rail-width: 84px`
- `--shell-panel-width: 320px`

Step 3 — Introduce the monospace stack the UI-SPEC reserves for numeric telemetry (it says explicitly that Phase 5 is when it arrives, and the Dashboard placeholder in Task 3 is that surface): declare `--font-mono: ui-monospace, "SF Mono", Consolas, monospace` alongside the type scale.

Step 4 — Flip the shadcn variable block. Leave the `@theme inline` block completely untouched (it is Tailwind's generated namespace). In the second `:root` block, remap every shadcn variable onto the new `--ui-*` roles so `:root` is itself the dark theme — there is no `.dark` class applied anywhere in this app, so do NOT try to drive this off the `.dark` block:

`--background` → `--ui-dominant`; `--foreground` → `--ui-fg`; `--card` and `--popover` → `--ui-surface` with their `-foreground` pair → `--ui-fg`; `--primary` → `--ui-accent` with `--primary-foreground` → `--ui-accent-fg`; `--secondary`, `--muted` and `--accent` → `--ui-surface-raised` with their `-foreground` pairs → `--ui-fg` (except `--muted-foreground` → `--ui-fg-muted`); `--destructive` → `--ui-destructive`; `--border` and `--input` → `--ui-border`; `--ring` → `--ui-accent`. Leave the `--chart-*` and `--sidebar-*` variables alone — nothing reads them.

Leave the trailing `.dark` block as-is. It is dead code (no `.dark` class is ever applied); removing it is out of scope for this pass.

Step 5 — Fix the hardcoded light background. The `body` rule near the top of the file hardcodes a near-white background; point it at `var(--ui-dominant)` instead.

Step 6 — Update the three TSX consumers to the new names. In `SampleSelect.tsx` and `ScrubControl.tsx`, the old role token was doing double duty as both a *surface* fill and a *text* color; split it correctly now — surfaces/borders/`accentColor` take `--ui-surface-raised` or `--ui-border`, and all the note/label text takes `--ui-fg-muted`. `SampleSelect.tsx`'s hardcoded near-black `select` text color becomes `var(--ui-fg)`. Update each file's existing UI-SPEC comment to name the new tokens rather than the old ones, so the comments do not go stale.

In `SceneStatusOverlay.tsx`, replace its two hardcoded literals — the light-grey panel constant and the near-black text/spinner color used in four places — with `var(--ui-surface)` and `var(--ui-fg)` respectively. For the spinner's translucent track border, use `var(--ui-border)` rather than an alpha-suffixed hex.

Step 7 — Keep the WebGL canvas in sync with the DOM. `CellScene.tsx` declares two tone constants with a comment stating they are deliberately kept in sync with the DOM background so the canvas and page chrome do not clash. Honour that invariant: set the dominant constant to `#1C1E21` and the secondary constant (floor plane, rail track, end-stops) to `#3C4149`. Change nothing else in that file — no lighting, no geometry, no composition order. The UR3e meshes are light grey and will read correctly against the dark floor without touching the robot material.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm test &amp;&amp; ! grep -rqE -- "--color-(dominant|secondary|accent|destructive)" src/ui src/scene &amp;&amp; grep -q -- "--ui-accent" src/index.css &amp;&amp; grep -q -- "--shell-rail-width" src/index.css</automated>
  </verify>

  <done>
`npm run build` (the real `tsc -b && vite build` gate) passes and the existing Vitest suite is still green. No file under `src/ui` or `src/scene` references a UI-SPEC role token under Tailwind's reserved color namespace any more. The page background, panel chrome and WebGL canvas all render dark grey; the Reset View button renders red.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tab registry, UI shell store, and the rail / mode-bar / panel-router chrome</name>

  <files>src/ui/tabs/tab-registry.ts, src/ui/tabs/tab-registry.test.ts, src/store/uiShellStore.ts, src/ui/shell/TabRail.tsx, src/ui/shell/ModeBar.tsx, src/ui/shell/TabPanel.tsx, src/ui/shell/PlaceholderPanel.tsx, src/App.tsx</files>

  <behavior>
`tab-registry.test.ts` asserts, against `TAB_DEFS`:
  - Test 1: it contains exactly 7 entries.
  - Test 2: the id set is exactly `dashboard, operations, setup, vision, calibrate, io, optimization`.
  - Test 3: every id is unique (no duplicate ids slipped in).
  - Test 4: every entry has a non-empty `label` and a `phase` number in the range 5-8 — so a tab can never ship without declaring which phase fills it.
  - Test 5: `DEFAULT_TAB_ID` is one of the registry ids.
  </behavior>

  <action>
Step 1 — Create `src/ui/tabs/tab-registry.ts` as the single source of truth for the tab set. Export a `TabId` union type and a `TAB_DEFS` readonly array of `{ id, label, phase, blurb }`, in this rail order:

  - `dashboard` / "Dashboard" / phase 5 / telemetry readouts
  - `operations` / "Operations" / phase 6 / operations tree and engagement legend
  - `setup` / "Setup" / phase 8 / cell device configuration
  - `vision` / "Vision" / phase 8 / simulated force and contact
  - `calibrate` / "Calibrate" / phase 8 / home and per-operation positions
  - `io` / "I/O" / phase 8 / digital I/O status
  - `optimization` / "Optimization" / phase 8 / feed-rate override and cycle time

Also export `DEFAULT_TAB_ID` as `'dashboard'`. This module holds data only — no JSX, no React import — so the test can import it without a DOM.

Step 2 — Write `src/ui/tabs/tab-registry.test.ts` covering the five assertions in the behavior block above, using the existing Vitest conventions in this repo (look at `src/store/cellStore.test.ts` for the house style).

Step 3 — Create `src/store/uiShellStore.ts` as a Zustand store **separate from `cellStore`**. `cellStore` holds simulation truth (toolpath, trajectory, scrub); this holds pure chrome state, and keeping them apart means a later phase can delete or rewire this store without touching simulation state. Shape:

  - `activeTab: TabId` initialised to `DEFAULT_TAB_ID`, with `setActiveTab(id)`
  - `cellMode: 'printing' | 'milling'` initialised to `'printing'`, with `setCellMode(mode)`

`cellMode` is visual-only this pass. It must not be read by anything under `src/scene/`, `src/trajectory/` or `src/kinematics/` — Phase 7 owns the real tool-changer swap.

Step 4 — Create `src/ui/shell/PlaceholderPanel.tsx` exporting the shared presentational primitives every panel in Task 3 composes from, so the seven panels stay consistent and none of them invents its own styling:

  - `PanelShell` — takes `title` and children; renders the panel heading at the UI-SPEC heading role plus a scrollable body.
  - `PanelSection` — takes a `heading` and children; a labelled group with a bottom hairline in `--ui-border`.
  - `ReadoutRow` — takes `label` and optional `value` (defaulting to an em-dash placeholder); renders label left in `--ui-fg-muted` at the label type role and value right in `--font-mono` at `--ui-fg`. This is the one primitive that makes "no data yet" read as deliberate rather than broken.
  - `PhaseNote` — takes `phase` and `children`; renders a muted footer line reading `Arrives in Phase {phase} — {children}`. Every panel ends with one.

Style these with inline `React.CSSProperties` objects referencing the `--ui-*` / `--space-*` / `--text-*` custom properties, exactly as `SampleSelect.tsx` and `ScrubControl.tsx` already do. Do not introduce a CSS-module or styled-components layer.

Step 5 — Create `src/ui/shell/TabRail.tsx`. A fixed-position vertical rail pinned to the left viewport edge at `width: var(--shell-rail-width)`, background `--ui-surface`, right border `--ui-border`, `zIndex: 2`. It maps over `TAB_DEFS` — never a second hardcoded list — rendering one `<button>` per tab with a `lucide-react` icon above the label at the UI-SPEC label type role. The active tab (read from `uiShellStore`) gets background `--ui-surface-raised`, text `--ui-fg`, and a 3px `--ui-accent` bar on its right edge; inactive tabs get `--ui-fg-muted`. Set `aria-current="page"` on the active button. Clicking dispatches `setActiveTab` and nothing else — this component never imports from `src/scene/`, matching the convention every existing control in `src/ui/` documents.

Step 6 — Create `src/ui/shell/TabPanel.tsx`. A fixed-position panel docked immediately right of the rail (`left: var(--shell-rail-width)`, `width: var(--shell-panel-width)`, top to bottom, `zIndex: 2`), background `--ui-surface`, right border `--ui-border`, `overflowY: auto`. It holds a `PANELS` map from `TabId` to a component and renders the entry for `activeTab`. Type the map as `Record<TabId, ComponentType>` so TypeScript itself fails the build if a registry id has no panel behind it. For this task, point all seven ids at a temporary inline stub that renders `PanelShell` with the tab's label and its `PhaseNote` — Task 3 replaces each entry with its real panel.

Step 7 — Create `src/ui/shell/ModeBar.tsx` (the Phase 7 surface, TOOL-01/TOOL-02). Fixed at `top: var(--space-lg)`, `left: calc(var(--shell-rail-width) + var(--shell-panel-width) + var(--space-lg))`, `zIndex: 2`. Renders a two-button segmented control — "Printing" and "Milling" — reading and dispatching `cellMode`; the selected segment fills `--ui-accent` with `--ui-accent-fg` text, the other sits on `--ui-surface-raised` with `--ui-fg-muted`. Use `role="tablist"` with `role="tab"` and `aria-selected` on the segments. Beside it, render a tool-changer chip reading `Mounted tool: Print head` or `Mounted tool: Mill spindle`, derived from `cellMode` at render time — never a second piece of state. Under the chip, a `PhaseNote` for phase 7 explaining that the tool-changer station swaps the mounted tool in the 3D scene there.

Step 8 — Wire the shell into `src/App.tsx`. Mount `TabRail`, `TabPanel` and `ModeBar` after `SceneStatusOverlay`. `CellScene` stays exactly as it is — a full-viewport `position: fixed` backdrop at `zIndex: 0` behind the shell; do not try to reflow it into a flex layout.

The pre-existing bottom-left overlay stack (`SampleSelect` + `ScrubControl`) currently sits at `left: var(--space-lg)` and would land underneath the new panel. Change its `left` to `calc(var(--shell-rail-width) + var(--shell-panel-width) + var(--space-lg))`, reusing the same tokens the shell's own geometry reads so the two can never drift apart. Leave the bottom-right `ResetViewButton` container untouched.
  </action>

  <verify>
    <automated>npm test -- tab-registry &amp;&amp; npm run build</automated>
  </verify>

  <done>
`tab-registry.test.ts` passes all five assertions. `npm run build` passes. Running `npm run dev` shows a dark-grey left rail with 7 icon+label tabs, a docked panel to its right whose heading changes when a tab is clicked, a red-highlighted active tab, and a Printing/Milling segmented control whose selection changes the mounted-tool chip text. The 3D scene, sample select and scrub control all still work, and the sample/scrub stack is fully visible to the right of the panel.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill the seven panels with phase-labelled placeholder content</name>

  <files>src/ui/tabs/DashboardPanel.tsx, src/ui/tabs/OperationsPanel.tsx, src/ui/tabs/SetupPanel.tsx, src/ui/tabs/VisionPanel.tsx, src/ui/tabs/CalibratePanel.tsx, src/ui/tabs/IoPanel.tsx, src/ui/tabs/OptimizationPanel.tsx, src/ui/shell/TabPanel.tsx, src/ui/tabs/tab-registry.test.ts</files>

  <behavior>
Extend `tab-registry.test.ts` with one further assertion:
  - Test 6: importing the `PANELS` map from `TabPanel.tsx`, every id in `TAB_DEFS` resolves to a defined panel component, and `PANELS` contains no key that is absent from `TAB_DEFS`. This is the gate that keeps the rail and the panel set from drifting apart as later phases add or rename tabs.
  </behavior>

  <action>
Create one panel component per tab under `src/ui/tabs/`, each composed purely from Task 2's `PanelShell` / `PanelSection` / `ReadoutRow` / `PhaseNote` primitives. Every panel is presentational: it takes no props, reads no store, computes nothing. Every control is rendered `disabled`. Every panel ends with a `PhaseNote`.

`DashboardPanel.tsx` (Phase 5 surface — DASH-01/02/03). Three sections:
  - "Joint angles" — six `ReadoutRow`s labelled Base, Shoulder, Elbow, Wrist 1, Wrist 2, Wrist 3, each showing a degree-suffixed em-dash placeholder.
  - "TCP" — four `ReadoutRow`s: X, Y, Z in mm, and Speed in mm/s.
  - "Rail — 7th axis" — three `ReadoutRow`s: Position, Travel remaining (−X), Travel remaining (+X), all in mm.
  - `PhaseNote` for phase 5 stating these become live once Phase 4 playback drives them.

`OperationsPanel.tsx` (Phase 6 surface — SIM-03/06/07). Two sections:
  - "Operations" — a static list of three disabled skeleton rows, each showing an operation-index label and an em-dash duration, prefixed by a small start/end marker glyph so the marker vocabulary is visible.
  - "Mill engagement" — a four-swatch legend running shallow to deep, using a red ramp derived from the accent (light to dark), labelled "Shallow" through "Deep". Mark it explicitly as a legend only, not a live reading.
  - `PhaseNote` for phase 6 covering per-operation markers, computed durations and engagement coloring.

`SetupPanel.tsx` (SETUP-01). One section, "Cell devices", with two disabled checkbox rows — "7th-axis rail" and "Tool-changer", both checked to reflect the cell as currently modelled. `PhaseNote` for phase 8.

`VisionPanel.tsx` (VISION-01). One section, "Force / contact", with a `ReadoutRow` showing a newton-suffixed em-dash and a flat static baseline bar beneath it. Carry a prominent muted line reading that the value is estimated and simulated, never measured from hardware — the roadmap's success criterion requires that disclosure, so bake it in now rather than retrofitting it. `PhaseNote` for phase 8.

`CalibratePanel.tsx` (CALIB-01/02). Two sections:
  - "Home position" — six disabled numeric inputs labelled J1 through J6, suffixed in degrees.
  - "Per-operation positions" — a disabled `<select>` with a single "No operation selected" option, plus a disabled shadcn `Button` (imported from `src/components/ui/button`) labelled "Preview pose".
  - `PhaseNote` for phase 8 mentioning the forward-kinematics pose preview.

`IoPanel.tsx` (IO-01). One section, "Digital I/O", listing three rows — "Tool-changer engaged", "Spindle on", "Extruder heating" — each with a leading inactive status dot in `--ui-fg-muted` and an em-dash state. `PhaseNote` for phase 8.

`OptimizationPanel.tsx` (OPT-01/02). Two sections:
  - "Feed-rate override" — a disabled range input from 10 to 200 with value 100 and a percentage readout beside it.
  - "Cycle time" — a `ReadoutRow` showing an em-dash placeholder.
  - `PhaseNote` for phase 8.

Then update `TabPanel.tsx`: replace the seven temporary stubs in the `PANELS` map with these real components, delete the stub, and export `PANELS` so the test can import it. Finally extend `tab-registry.test.ts` with the Test 6 assertion described in the behavior block.

No panel may import the simulation store (`src/store/cellStore.ts`), and no panel may compute a placeholder value from the trajectory. Playback does not exist yet; a placeholder that silently reads half-real data is worse than an em-dash, because it looks live and is not. The Task 3 gate greps for `import` lines only, so explaining this restriction in a code comment is fine and will not trip it.
  </action>

  <verify>
    <automated>npm test &amp;&amp; npm run build &amp;&amp; ! grep -rqE "^\s*import .*cellStore" src/ui/tabs</automated>
  </verify>

  <done>
All seven tabs render their own distinct placeholder panel with the correct sections, every interactive control is disabled, and every panel shows an "Arrives in Phase N" footer. `tab-registry.test.ts` proves every registry id has a panel and no orphan panel exists. `npm test` and `npm run build` both pass, and no panel imports the simulation store.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no network calls, no file input, no user-supplied data path, and no new dependency. All state is local, in-memory, developer-authored chrome state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Information disclosure | Placeholder panels | low | mitigate | Every placeholder renders a developer-authored em-dash constant and an "Arrives in Phase N" note; panels are forbidden from importing the simulation store, gated by the Task 3 import-line grep, so no partially-computed simulation value can leak out as an apparently-live readout. |
| T-quick-02 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan — the shell is built from native controls plus the already-installed `src/components/ui/button.tsx` and `lucide-react`. No package legitimacy audit is required because no new package enters the dependency tree. If an executor finds itself reaching for `npx shadcn add`, stop and raise it instead. |
</threat_model>

<verification>
1. `npm run build` passes — this is the real gate (`tsc -b && vite build`); STATE.md records that a bare `tsc --noEmit` silently misses failures in this repo.
2. `npm test` passes, including the new `tab-registry.test.ts`.
3. `npm run dev` and confirm visually: dark-grey page and canvas, red accents only on the active tab bar / selected mode segment / Reset View button, 7 tabs each swapping the panel, mode switch changing the mounted-tool chip.
4. Phase 1-3 regression: the UR3e still renders, selecting a g-code sample still draws its toolpath, and dragging the scrub control still re-poses the robot — with the sample/scrub stack fully visible beside the new panel, not hidden behind it.
</verification>

<success_criteria>
- All 7 tabs (Dashboard, Operations, Setup, Vision, Calibrate, I/O, Optimization) exist in the rail and render a distinct placeholder panel.
- Printing/Milling mode switcher and tool-changer chip render and respond to clicks (visual state only).
- Every panel names the phase that will make it real; no placeholder can be read as live data.
- Palette is Red + Dark Grey across DOM chrome and WebGL canvas alike.
- Zero new npm dependencies; zero changes to `src/kinematics/`, `src/trajectory/`, `src/gcode/`, or `src/store/cellStore.ts`.
- `npm run build` and `npm test` both pass.
</success_criteria>

<output>
Create `.planning/quick/260815-3cn-skip-phase-4-playback-engine-for-now-cre/260815-3cn-SUMMARY.md` when done.
</output>
