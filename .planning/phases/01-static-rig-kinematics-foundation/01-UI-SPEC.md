---
phase: 1
slug: static-rig-kinematics-foundation
status: draft
shadcn_initialized: false
preset: neutral (default — CSS variables enabled)
created: 2026-08-13
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | Default — `npx shadcn init` standard interactive defaults (neutral base color, CSS variables enabled). No custom preset URL from ui.shadcn.com/create was supplied; user explicitly confirmed the default neutral/slate preset. Not yet run — no `package.json`/`components.json` exists in the repo yet (Phase 1 is the project's first implementation phase; scaffolding itself, including `npx shadcn init`, is part of Phase 1's build work per `01-RESEARCH.md`'s installation sequence). |
| Component library | Radix UI primitives (via shadcn/ui) |
| Icon library | `lucide-react` (shadcn/ui's default icon set) |
| Font | System UI stack (`-apple-system, "Segoe UI", Roboto, sans-serif` — shadcn/ui's default, no custom webfont). No branded typeface was requested for this phase; revisit only if a later phase introduces brand identity requirements. |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing (e.g. nav-cube face padding) |
| md | 16px | Default element spacing |
| lg | 24px | Section padding (e.g. Reset View button container) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: This is a desktop-first CAD/robotics viewer (no touch-primary interaction expected), so the standard 44px minimum touch-target guidance is relaxed for the nav-cube's individual face/edge/corner hit-regions and the small camera-control icon buttons, which may use a minimum 32px hit area instead. The primary "Reset View" CTA button remains a standard-sized button (not icon-only) and follows normal spacing/padding tokens above.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label | 14px | 500 (medium) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.2 |

3 font weights are declared for this phase, each with a distinct, non-overlapping semantic role: 400 (regular) for body prose, 500 (medium) reserved exclusively for compact UI labels (nav-cube face labels, button labels — never body text or headings), and 600 (semibold) for headings/display. No element may use more than one weight, and no weight is used outside its assigned role.

Monospace is reserved for numeric telemetry readouts (joint angles, TCP coordinates, speed) and is **not used in Phase 1** — this phase has no live numeric readouts (Dashboard telemetry begins Phase 5). Note for future phases: when introduced, use a monospace stack (e.g. `ui-monospace, "SF Mono", Consolas, monospace`) at 14px/500/1.4 to match the Label role's rhythm.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FAFAFA` | Page background, scene backdrop/canvas clear color, default surfaces |
| Secondary (30%) | `#E4E7EB` | Floor plane base tone, rail track/end-stop geometry, any chrome panels or overlay backgrounds (e.g. loading/error overlay panel) |
| Accent (10%) | `#2563EB` | Reserved for exactly: (1) the nav-cube's active/hovered face highlight, (2) the "Reset View" button (fill or prominent border, as the phase's one primary CTA), (3) camera-control active/toggled states if any exist beyond `OrbitControls` itself. Nothing else — not the robot mesh, not the rail geometry, not general body links (none exist in Phase 1). |
| Destructive | `#DC2626` | Reserved for destructive actions only. **Unused in Phase 1** — no destructive actions exist in this phase (view-only 3D scene, no delete/reset-that-discards-data operations). Documented now so later phases inherit a consistent destructive token rather than inventing one ad hoc. |

Accent reserved for: nav-cube active/hover face highlight; "Reset View" CTA button; camera-control active/toggled states (if implemented beyond bare `OrbitControls`). Never applied to the robot/rail 3D geometry itself, background chrome, or as a general link/text color.

Note on D-06 (neutral studio lighting): the 3D scene's own lighting (soft ambient + directional light) and background (light gray/white, or a subtle gradient within the Dominant/Secondary range above) are Three.js scene properties, not CSS/DOM color tokens — they should read as visually consistent with the Dominant (#FAFAFA) / Secondary (#E4E7EB) palette above so the DOM chrome and the WebGL canvas don't clash, but exact gradient stops are implementation discretion.

**Visual focal point:** The UR3e robot model (on its rail carriage) is the primary visual anchor of the scene — it must read as the largest, most detailed, and most central element in the frame (default camera framing centers the robot with comfortable margin, per D-08/D-09 pose and carriage-centering). The nav-cube (top corner) and the "Reset View" button are secondary controls: small, positioned at the scene's periphery, and styled with restrained accent use (per the Accent row above) so they orient/assist without competing with the robot for attention. Floor plane, rail track, and end-stop markers stay in the Secondary (30%) tone specifically so they recede behind the robot rather than drawing the eye.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Reset View" (SCENE-02 — resets the camera to the centered starting view) |
| Loading state | "Loading robot model…" — shown with a spinner overlay while `urdf-loader` fetches and parses the UR3e URDF + meshes |
| Empty state | Not applicable — Phase 1 has no user data collections or empty-list states. The robot model is a bundled build-time asset that is always present; the only asynchronous states are Loading and Error (above/below). |
| Error state | "Couldn't load the robot model. Check your connection and reload the page." — shown in place of the 3D scene if `urdf-loader`'s load callback fails or the URDF/mesh fetch errors |
| Destructive confirmation | Not applicable — no destructive actions exist in Phase 1 (view-only 3D scene; camera reset restores a view, it does not delete or discard any user data). |

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe (Step 9.5) and lifted by plan-phase's
> `## UI Considerations` lift rule via the identical rule as SPEC `## Edge Coverage`. Shape-rooted UI *state*
> coverage (empty / loading / error / populated / partial / overflow / zero-one-many / long-text).
> Empty-state and error-state COPY live in `## Copywriting Contract` above — this section covers
> state coverage and REFERENCES those rows rather than restating the copy (de-dup).

Applicable state considerations resolved: 3 covered, 0 backstop, 0 unresolved (4 additional considerations classified and explicitly dismissed as not-applicable — see reasons below).

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| loading | 3D scene / robot model (media) | ✅ covered | "Loading robot model…" spinner overlay (see Copywriting Contract → Loading state) shown while `urdf-loader` fetches/parses URDF + meshes |
| error | 3D scene / robot model (media) | ✅ covered | "Couldn't load the robot model. Check your connection and reload the page." (see Copywriting Contract → Error state) shown if the load callback fails |
| populated | 3D scene / robot model (media) | ✅ covered | Happy-path is the UR3e rendered in the D-08 slightly-bent "ready" pose, rail carriage centered on its travel range (D-09), against the neutral studio-lit backdrop (D-06), with end-stop markers visible on the track (D-07) |

**Dismissed (classified, explicitly not applicable — reason string recorded per probe protocol):**

- **empty** — 3D scene / robot model (media): the robot model is a bundled, build-time static asset (URDF + meshes shipped in `public/`) that is always present once the app runs — there is no "zero data" input state to design for, unlike user-uploaded content (g-code upload's empty state is Phase 2's concern, not this phase's).
- **loading** — Reset View button / nav-cube (interactive-control): both are synchronous, local Three.js camera transforms (`OrbitControls.reset()` / `tweenCamera`) with no network or async data dependency; no loading state exists to design.
- **error** — Reset View button / nav-cube (interactive-control): deterministic client-side camera operations with no failure mode (no network call, no data validation that can reject).
- **long-text** — Reset View button label / nav-cube face labels (interactive-control): all labels are fixed, short, developer-authored strings ("Reset View", "FRONT"/"TOP"/"BOTTOM"/"BACK"/"LEFT"/"RIGHT"), never user-generated or dynamically sized — no truncation/wrapping risk exists.

No `nav`, `form`, or `list-collection` elements exist in Phase 1's UI surface (tab bar, operations tree, and g-code upload form are later-phase scope per `ROADMAP.md`), so their associated consideration categories were not raised this phase.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | `Button` (Reset View CTA) | not required |
| third-party | none | not applicable |

No third-party registries were requested or declared for this phase. The loading/error overlay is a plain styled overlay (not a shadcn primitive) layered over the R3F `<Canvas>` — shadcn/ui has no dedicated spinner component in its official registry; use a minimal custom CSS spinner or an inline SVG, not a third-party block, to keep the registry surface at "shadcn official only" for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
