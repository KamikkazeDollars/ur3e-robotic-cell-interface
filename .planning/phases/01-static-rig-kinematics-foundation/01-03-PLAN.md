---
phase: 01-static-rig-kinematics-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - components.json
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.app.json
  - vite.config.ts
  - src/index.css
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/scene/camera-defaults.ts
  - src/scene/CameraResetListener.tsx
  - src/scene/CellScene.tsx
  - src/store/cellStore.ts
  - src/store/cellStore.test.ts
  - src/ui/ResetViewButton.tsx
  - src/App.tsx
autonomous: false
requirements: [SCENE-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "User can return the camera to the default centered view of the robot cell with a single click of one visible control, from any orbit/pan/zoom state (SCENE-02)"
    - "The camera's initial framing and the framing restored by the reset control come from one shared exported constant, so 'reset' provably returns to the starting view rather than to a re-typed approximation of it (SCENE-02)"
    - "The reset action can be triggered repeatedly and re-fires every time, rather than working only once per page load (SCENE-02)"
    - "A shadcn/ui design-system layer is initialised with the neutral preset and CSS variables, and the UI-SPEC spacing, typography, and colour tokens are declared once in the stylesheet for every later phase's tabs and panels to consume"
    - "The reset control renders at the UI-SPEC Label size in the semibold weight and carries the Accent tone as the phase's one primary DOM call-to-action"
    - "The Vitest harness installed in plan 01-01 still runs green after the design-system CLI rewrites the Vite config, so plan 01-02's kinematics suite is not silently orphaned"
  artifacts:
    - path: "src/scene/camera-defaults.ts"
      provides: "The single source of truth for the scene's default camera framing — read by both the initial camera and the reset action"
      exports: ["DEFAULT_CAMERA_POSITION", "DEFAULT_CAMERA_TARGET"]
    - path: "src/store/cellStore.ts"
      provides: "Minimal Zustand store — the coarse, UI-cadence cell state later phases extend (never per-frame values)"
      exports: ["useCellStore"]
    - path: "src/store/cellStore.test.ts"
      provides: "Framework-free store assertions proving the reset action re-fires on every invocation"
      contains: "requestCameraReset"
    - path: "src/scene/CameraResetListener.tsx"
      provides: "In-Canvas subscriber that restores the default framing on the default OrbitControls when the store's reset token changes"
      contains: "DEFAULT_CAMERA_POSITION"
    - path: "src/ui/ResetViewButton.tsx"
      provides: "The phase's one primary DOM CTA — a shadcn Button that dispatches the store's reset action"
      contains: "Reset View"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn/ui official Button block — the only registry block this phase installs"
      contains: "buttonVariants"
    - path: "components.json"
      provides: "shadcn/ui project manifest recording the neutral preset and CSS-variables mode"
      contains: "neutral"
    - path: "src/index.css"
      provides: "The design-token layer — UI-SPEC spacing, typography, and colour declared once as CSS custom properties"
      contains: "--space-lg"
  key_links:
    - from: "src/ui/ResetViewButton.tsx"
      to: "src/store/cellStore.ts"
      via: "the button's click handler dispatches the store's reset action; it never reaches into the 3D scene directly"
      pattern: "requestCameraReset"
    - from: "src/store/cellStore.ts"
      to: "src/scene/CameraResetListener.tsx"
      via: "the listener subscribes to the store's reset token and acts when it changes"
      pattern: "resetToken"
    - from: "src/scene/CameraResetListener.tsx"
      to: "src/scene/camera-defaults.ts"
      via: "the listener restores the same exported framing constant the initial camera was built from"
      pattern: "DEFAULT_CAMERA_POSITION"
    - from: "src/scene/CellScene.tsx"
      to: "src/scene/camera-defaults.ts"
      via: "the initial PerspectiveCamera position and the OrbitControls target are read from the shared constant, not inlined"
      pattern: "DEFAULT_CAMERA_TARGET"
  prohibitions:
    - requirement_id: "SCENE-02"
      category: "transparency"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT present the reset control as an action that discards, clears, or reloads anything the user has loaded — it restores camera framing only, and must not unload the scene, reset the robot pose, or reset any non-camera state the user established."
    - requirement_id: "SCENE-02"
      category: "safety"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT move the camera on its own — the default framing may only be restored in response to an explicit user action, never automatically on a timer, on a store change unrelated to the reset action, or while the user is mid-drag."
---

<objective>
Give the scene its design-system layer and its one-action camera reset: initialise shadcn/ui with the UI-SPEC's neutral preset, declare the UI-SPEC spacing/typography/colour tokens once as CSS custom properties, and wire a "Reset View" button through a minimal Zustand store to a shared default-framing constant that both the initial camera and the reset path read.

Purpose: SCENE-02 is the one Phase 1 requirement no existing plan covers. It is also the phase's only DOM interaction, which makes it the natural place to stand up the design-system layer that every later phase's tabs, dashboard, and operations tree will be authored against — plan 01-01 deliberately left `src/index.css` minimal and deferred that layer here. Deriving the reset target from one exported constant (rather than a second, hand-copied camera position) is what makes "reset" provably return to the starting view instead of somewhere that merely looks similar.

Output: a running, deployed scene with a working Reset View control; `components.json` + the token layer + the official shadcn Button; and the minimal Zustand store whose shape Phases 5-8 extend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-static-rig-kinematics-foundation/01-CONTEXT.md
@.planning/phases/01-static-rig-kinematics-foundation/01-UI-SPEC.md
@.planning/phases/01-static-rig-kinematics-foundation/01-RESEARCH.md
@.planning/phases/01-static-rig-kinematics-foundation/01-PATTERNS.md
@.planning/phases/01-static-rig-kinematics-foundation/01-VALIDATION.md
@.planning/phases/01-static-rig-kinematics-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package-legitimacy gate for the design-system dependency set</name>
  <what-built>Nothing yet — this is a package-legitimacy gate that must clear before the design-system CLI runs. `01-RESEARCH.md`'s Package Legitimacy Audit covered the 15 packages plan 01-01 installs; it does **not** cover the dependency set the shadcn/ui CLI pulls in. Under the planner's package-legitimacy protocol, packages absent from the audit table are treated as `[ASSUMED]` and require a blocking human verification before install. This gate is not auto-approvable.</what-built>
  <how-to-verify>
    Open each package page below and confirm, for each one, that (a) the page exists, (b) the repository link points at the canonical org named in brackets, and (c) weekly downloads are in the millions (or, for `tw-animate-css`, that it is the shadcn-recommended animation package with a plausible download volume and a real source repo — it is the smallest and newest of the set and deserves the closest look):

    1. https://www.npmjs.com/package/shadcn — [shadcn-ui/ui]
    2. https://www.npmjs.com/package/tailwindcss — [tailwindlabs/tailwindcss]
    3. https://www.npmjs.com/package/@tailwindcss/vite — [tailwindlabs/tailwindcss]
    4. https://www.npmjs.com/package/class-variance-authority — [joe-bell/cva]
    5. https://www.npmjs.com/package/clsx — [lukeed/clsx]
    6. https://www.npmjs.com/package/tailwind-merge — [dcastil/tailwind-merge]
    7. https://www.npmjs.com/package/lucide-react — [lucide-icons/lucide]
    8. https://www.npmjs.com/package/tw-animate-css — [Wombosvideo/tw-animate-css]
    9. https://www.npmjs.com/package/@radix-ui/react-slot — [radix-ui/primitives]

    If the CLI's actual dependency set differs from this list when it runs, the executor must stop and re-raise this gate with the delta rather than installing unlisted packages.
  </how-to-verify>
  <resume-signal>Type "approved" to authorise the design-system install, or name any package that did not check out</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Initialise the shadcn/ui design system and declare the UI-SPEC token layer</name>
  <reversibility rating="costly">shadcn/ui + Tailwind becomes the styling substrate every later phase's tabs, Dashboard panels, operations tree, and I/O list are authored against. Replacing it after Phase 5 means restyling every panel, not swapping one module.</reversibility>
  <files>components.json, package.json, package-lock.json, tsconfig.json, tsconfig.app.json, vite.config.ts, src/index.css, src/lib/utils.ts, src/components/ui/button.tsx</files>
  <read_first>
    - `.planning/phases/01-static-rig-kinematics-foundation/01-UI-SPEC.md` — the whole file, but especially "Design System" (shadcn/ui, neutral preset, CSS variables enabled, system-UI font stack, `lucide-react` icons), "Spacing Scale" (the seven 4px-multiple tokens and the desktop-first 32px hit-area relaxation), "Typography" (the hard two-weight cap and the role-to-weight-by-function rule), "Color" (the four role values and the Accent reservation list), and "Registry Safety" (shadcn official only; `Button` is the one block; the status overlay must not come from a third-party registry)
    - `.planning/phases/01-static-rig-kinematics-foundation/01-01-PLAN.md` — the `files_modified` list and Task 1 Step 1/Step 3, so the pinned versions and the minimal `src/index.css` this task builds on are known
    - `.planning/phases/01-static-rig-kinematics-foundation/01-01-SUMMARY.md` — the recorded production URL and GitHub repo, and whatever the executor actually pinned
    - `vite.config.ts` and `tsconfig.app.json` as they exist on disk — the Vitest `test` block and the compiler options the CLI is about to rewrite
    - `.planning/phases/01-static-rig-kinematics-foundation/01-RESEARCH.md` — "Standard Stack → Core" (the Vite 7.x / TypeScript 5.9.3 pins the CLI must not disturb) and "Package Legitimacy Audit" (why the gate above exists)
  </read_first>
  <action>
Run the shadcn/ui CLI init **first**, before hand-authoring anything else in this plan, because it rewrites `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, and `src/index.css` wholesale and will clobber hand edits made ahead of it.

**Step 1 — Initialise.** Run the shadcn CLI's init command against this project, selecting the neutral base colour with CSS variables enabled (UI-SPEC "Design System": these are the standard interactive defaults; the user explicitly confirmed them and no custom preset URL was supplied). The CLI installs Tailwind and its Vite plugin plus the utility packages cleared by the gate above, writes `components.json`, creates `src/lib/utils.ts`, rewrites `src/index.css` with the Tailwind import and its token block, registers the Tailwind Vite plugin in `vite.config.ts`, and adds the `@/*` path alias to the TypeScript config plus a matching resolve alias in the Vite config.

Immediately afterwards, audit what it changed:
- Confirm the Vitest `test` block plan 01-01 put in `vite.config.ts` survived the rewrite. If the CLI dropped it, restore it — plan 01-02's kinematics suite runs through that config, and losing it silently orphans the phase's only automated correctness gate.
- Confirm the TypeScript version is still on the 5.9 line and Vite is still on the 7.x line. If the CLI's install pulled either forward, re-pin them exactly as plan 01-01 did.
- Confirm the resolve alias in the Vite config and the `paths` entry in the TypeScript config agree on the same alias prefix, so type-checking and bundling resolve imports identically.

**Step 2 — Add the one registry block.** Add the official shadcn `Button` block. Per UI-SPEC "Registry Safety" this is the only block installed this phase and the only registry consulted; do not add a third-party registry or a spinner block. Review the generated `src/components/ui/button.tsx` before committing it — it is registry-authored source landing directly in the source tree, so it gets read, not rubber-stamped.

**Step 3 — Declare the UI-SPEC token layer.** In `src/index.css`, alongside (not replacing) the CLI's own token block, declare the UI-SPEC contract as CSS custom properties so later phases inherit one vocabulary instead of re-deriving it:
- Spacing, all multiples of four: `--space-xs` 4px, `--space-sm` 8px, `--space-md` 16px, `--space-lg` 24px, `--space-xl` 32px, `--space-2xl` 48px, `--space-3xl` 64px.
- Type scale, exactly two weights: `--text-body` 16px with `--leading-body` 1.5, `--text-label` 14px with `--leading-label` 1.4, `--text-heading` 20px and `--text-display` 28px both with `--leading-tight` 1.2; `--weight-regular` 400 and `--weight-semibold` 600. Declare no third weight — the UI-SPEC caps this phase at two and differentiates Body from Label by size and line-height alone.
- Colour roles: `--color-dominant` set to the UI-SPEC Dominant value `#FAFAFA`, `--color-secondary` to `#E4E7EB`, `--color-accent` to `#2563EB`, and `--color-destructive` to `#DC2626`. The destructive role is declared but applied nowhere this phase — the UI-SPEC records it now specifically so later phases inherit one token instead of inventing their own.
Then map the CLI's own primary and background variables onto the Accent and Dominant tokens, so the generated Button renders as the phase's one accent CTA without per-component colour overrides, and the page background matches the WebGL canvas clear colour plan 01-01 set.

Set the base font stack to the system-UI stack named in the UI-SPEC — no webfont is loaded this phase.

**Step 4 — Confirm the scene survived the reset.** Tailwind's preflight applies a global element reset that plan 01-01's full-viewport canvas layout has not seen before. Run the dev server or a production preview and confirm the 3D scene still fills the viewport rather than collapsing to zero height; if it collapsed, give the canvas container an explicit full-height rule in `src/index.css` rather than reintroducing inline styles.

Do not build the Reset View control in this task — task 2 owns it.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; test -f components.json &amp;&amp; test -f src/lib/utils.ts &amp;&amp; test -f src/components/ui/button.tsx &amp;&amp; node -e "const fs=require('fs');const fail=m=>{console.error(m);process.exit(1)};const vc=fs.readFileSync('vite.config.ts','utf8');if(!/\btest\s*:/.test(vc))fail('Vitest test block was lost from vite.config.ts');const css=fs.readFileSync('src/index.css','utf8');for(const t of ['--space-xs','--space-sm','--space-md','--space-lg','--space-xl','--space-2xl','--space-3xl','--text-body','--text-label','--text-heading','--text-display','--weight-regular','--weight-semibold','--color-dominant','--color-secondary','--color-accent','--color-destructive'])if(!css.includes(t))fail('missing UI-SPEC token: '+t);const cj=fs.readFileSync('components.json','utf8');if(!/neutral/.test(cj))fail('components.json does not record the neutral preset');console.log('DESIGN_SYSTEM_OK')" &amp;&amp; npx tsc --version | grep -E 'Version 5\.9\.' &amp;&amp; npm ls vite | grep -E 'vite@7\.'</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists and records the `neutral` base colour.
    - `src/lib/utils.ts` and `src/components/ui/button.tsx` exist; `button.tsx` exports `buttonVariants`.
    - `src/index.css` declares all seven `--space-*` tokens, all four `--text-*` tokens, both `--weight-*` tokens, and all four `--color-*` role tokens, with the four colour values set to the UI-SPEC Dominant/Secondary/Accent/Destructive hex values.
    - `vite.config.ts` still contains a `test` key — the Vitest harness survived the CLI rewrite.
    - `npx vitest run` exits 0 (plan 01-02's kinematics suite still runs and still passes through the rewritten config).
    - `npx tsc --version` reports a `5.9.` version and `npm ls vite` shows a `vite@7.` resolution — the CLI's install did not move either pin.
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
    - The only registry block added is `Button`; no third-party registry is configured in `components.json`.
  </acceptance_criteria>
  <done>The project has a shadcn/ui design system on the neutral preset, the UI-SPEC spacing/typography/colour contract is declared once as CSS custom properties, the official Button block is installed, and the build, type-check, and existing kinematics test suite are all still green afterwards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One-action camera reset, wired button through store to the shared default framing</name>
  <reversibility rating="reversible">The store's reset-token shape and the default-framing constants are local to the camera concern; replacing the mechanism later touches three small files and no other subsystem.</reversibility>
  <files>src/scene/camera-defaults.ts, src/scene/CameraResetListener.tsx, src/scene/CellScene.tsx, src/store/cellStore.ts, src/store/cellStore.test.ts, src/ui/ResetViewButton.tsx, src/App.tsx</files>
  <read_first>
    - `src/scene/CellScene.tsx` as plan 01-01 wrote it — the `PerspectiveCamera` framing values to lift into constants, the `OrbitControls` element carrying `makeDefault`, and the mount points already named in it
    - `src/App.tsx` as plan 01-01 wrote it — how `CellScene` is mounted as the full-viewport surface, so the DOM control can be layered over it without disturbing the canvas
    - `.planning/phases/01-static-rig-kinematics-foundation/01-UI-SPEC.md` — "Copywriting Contract" (the CTA copy is exactly `Reset View`), "Color" (Accent is reserved for the nav cube and this CTA and nothing else; the robot must stay the visual focal point and this control must sit at the periphery), "Typography" (this CTA is Label size at semibold weight because it is actionable, not passive), and "Spacing Scale" (`lg` / 24px is the Reset View container's padding; the standard touch-target minimum is relaxed but this CTA is a normal-sized button, not icon-only)
    - `.planning/phases/01-static-rig-kinematics-foundation/01-PATTERNS.md` — the `src/scene/NavCube.tsx` + camera-wiring section, specifically the "Camera reset (SCENE-02)" paragraph, and the `src/store/cellStore.ts` section with its hard constraint
    - `.planning/phases/01-static-rig-kinematics-foundation/01-RESEARCH.md` — the SCENE-02 row of the Phase Requirements table, and "Anti-Patterns to Avoid" plus the Zustand row of Standard Stack (coarse UI-cadence state only)
    - `.planning/phases/01-static-rig-kinematics-foundation/01-VALIDATION.md` — the SCENE-02 row of "Manual-Only Verifications", which is why this task's automated gate targets the store and the wiring rather than the visual outcome
  </read_first>
  <behavior>
    - `useCellStore.getState().resetToken` starts at `0`.
    - Calling `useCellStore.getState().requestCameraReset()` once leaves `resetToken` at `1`.
    - Calling it three times in a row leaves `resetToken` at `3` — the value strictly increases on every call, so a second and third reset re-fire rather than being swallowed as a no-op repeat of the same state.
    - `requestCameraReset()` changes `resetToken` and nothing else: every other key on the store snapshot is identical before and after the call.
    - `DEFAULT_CAMERA_POSITION` and `DEFAULT_CAMERA_TARGET` are each three-number tuples, and they are not equal to each other — the camera sits away from the point it looks at, so the default framing is a real view rather than a degenerate one.
  </behavior>
  <action>
Write `src/store/cellStore.test.ts` first from the behaviours above and confirm it fails before implementing — the store is the mechanically-checkable half of SCENE-02 and it must not be written to match whatever the implementation happens to do.

Create `src/scene/camera-defaults.ts` exporting `DEFAULT_CAMERA_POSITION` and `DEFAULT_CAMERA_TARGET` as `as const` three-number tuples in metres, lifted from the framing plan 01-01 already chose in `CellScene.tsx` (the target is the point the cell is centred on; the position is the eye point that frames a roughly 1.5 m-tall rig on a ~3 m rail with comfortable margin). This file is the single source of truth for the phase's default view: nothing else may restate those numbers, because "reset returns to the starting view" is only provable if the starting view and the reset target are literally the same values.

Create `src/store/cellStore.ts` exporting `useCellStore`, a Zustand store holding `resetToken: number` (starting at 0) and `requestCameraReset(): void`, which increments it. Model the reset as a monotonically increasing token rather than a boolean flag so repeated resets from the same camera state each produce a distinct value and therefore each fire — a boolean would need a manual clear and would silently swallow the second click. Per the project's hard constraint, this store carries coarse UI-cadence state only: no joint angles, no camera position, no TCP values, nothing that would change on a render frame. Keep the Three.js camera and controls objects out of the store entirely — the store carries the intent, the scene carries the objects.

Create `src/scene/CameraResetListener.tsx`, a component rendered inside the R3F Canvas that returns nothing visible. It reads the default controls that plan 01-01's `OrbitControls` registered through `makeDefault` and the active camera from the R3F state, subscribes to `resetToken` from `useCellStore`, and on every change after the initial mount restores the camera position from `DEFAULT_CAMERA_POSITION`, sets the controls' orbit target from `DEFAULT_CAMERA_TARGET`, and calls the controls' update method so the change takes effect on the next frame. Guard against the controls being absent on first render. Do not act on mount — the camera is already at the default framing then, and moving it unprompted would be a camera move the user did not ask for.

Edit `src/scene/CellScene.tsx` so its `PerspectiveCamera` position and its `OrbitControls` target are both read from `camera-defaults.ts` rather than inlined, and mount `CameraResetListener` inside the Canvas alongside the existing `NavCube`. Leave the `makeDefault` prop and the rail mount point plan 01-01 left for plan 01-04 exactly as they are.

Create `src/ui/ResetViewButton.tsx`: a DOM component (outside the Canvas) rendering the shadcn `Button` with the copy `Reset View` exactly as the UI-SPEC Copywriting Contract states, styled at the Label type size in the semibold weight and carrying the Accent tone as the phase's one primary DOM call-to-action. Its click handler calls `requestCameraReset` from the store and does nothing else — it must not touch the scene, the camera, or the robot. Mount it in `src/App.tsx` as an overlay pinned to a bottom corner of the viewport over the canvas, with `lg` (24px) padding from the viewport edges, so it reads as a peripheral secondary control and the robot stays the frame's focal point per the UI-SPEC.

Finally commit and push, so the connected deployment rebuilds, and confirm the production URL recorded in `README.md` serves the updated bundle before handing off to the checkpoint.
  </action>
  <verify>
    <automated>npx vitest run src/store/cellStore.test.ts &amp;&amp; npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node -e "const fs=require('fs');const fail=m=>{console.error(m);process.exit(1)};const cs=fs.readFileSync('src/scene/CellScene.tsx','utf8');const rl=fs.readFileSync('src/scene/CameraResetListener.tsx','utf8');const rb=fs.readFileSync('src/ui/ResetViewButton.tsx','utf8');const ap=fs.readFileSync('src/App.tsx','utf8');const st=fs.readFileSync('src/store/cellStore.ts','utf8');if(!cs.includes('DEFAULT_CAMERA_POSITION'))fail('CellScene does not read the shared default framing');if(!cs.includes('DEFAULT_CAMERA_TARGET'))fail('CellScene does not read the shared default target');if(!cs.includes('CameraResetListener'))fail('CameraResetListener is not mounted in the Canvas');if(!rl.includes('DEFAULT_CAMERA_POSITION')||!rl.includes('DEFAULT_CAMERA_TARGET'))fail('reset listener does not restore the shared default framing');if(!rl.includes('resetToken'))fail('reset listener does not subscribe to the store token');if(!rb.includes('requestCameraReset'))fail('Reset View button is not wired to the store action');if(!rb.includes('Reset View'))fail('Reset View CTA copy is missing');if(!ap.includes('ResetViewButton'))fail('ResetViewButton is not mounted in App');for(const banned of ['jointAngle','tcpPosition','cameraPosition'])if(st.includes(banned))fail('per-frame value leaked into the store: '+banned);console.log('SCENE_02_WIRING_OK')" &amp;&amp; URL=$(grep -oE 'https://[A-Za-z0-9.-]+\.vercel\.app' README.md | head -1) &amp;&amp; test -n "$URL" &amp;&amp; curl -sSf "$URL" -o "${TMPDIR:-/tmp}/gsd-0103-index.html" &amp;&amp; ASSET=$(grep -oE '/assets/[A-Za-z0-9._-]+\.js' "${TMPDIR:-/tmp}/gsd-0103-index.html" | head -1) &amp;&amp; test -n "$ASSET" &amp;&amp; curl -sSf -o /dev/null "$URL$ASSET" &amp;&amp; echo RESET_VIEW_DEPLOYED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/store/cellStore.test.ts` exits 0 with a named test for each behaviour above, including the three-consecutive-calls case that proves the token strictly increases.
    - `src/scene/camera-defaults.ts` exports `DEFAULT_CAMERA_POSITION` and `DEFAULT_CAMERA_TARGET`.
    - `src/scene/CellScene.tsx` references both `DEFAULT_CAMERA_POSITION` and `DEFAULT_CAMERA_TARGET` and mounts `CameraResetListener`; its `OrbitControls` still carries `makeDefault`.
    - `src/scene/CameraResetListener.tsx` references `resetToken` and both default-framing constants.
    - `src/ui/ResetViewButton.tsx` contains the literal CTA copy `Reset View` and calls `requestCameraReset`.
    - `src/App.tsx` mounts `ResetViewButton`.
    - `src/store/cellStore.ts` contains no joint-angle, TCP, or camera-position field — the store holds intent, not per-frame values.
    - `npx vitest run` (full suite), `npx tsc --noEmit`, and `npm run build` all exit 0.
    - The production URL from `README.md` returns 200 and the `/assets/*.js` bundle its HTML references also returns 200 — the reset control is live, not just local.
  </acceptance_criteria>
  <done>Clicking one visible "Reset View" control on the deployed app returns the camera to exactly the framing the scene opened with, from any orbit/pan/zoom state and as many times as the user clicks, driven by a store token and a single shared framing constant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm Reset View and the token layer on the live URL (SCENE-02)</name>
  <what-built>A shadcn/ui design-system layer on the neutral preset with the UI-SPEC spacing/typography/colour tokens declared as CSS custom properties, and a "Reset View" button pinned to a bottom corner of the viewport that restores the scene's default camera framing through a Zustand reset token.</what-built>
  <how-to-verify>
    1. Open the production URL recorded under "Live deployment" in `README.md`. The 3D scene must still fill the viewport with the floor plane visible — Tailwind's global element reset landed in this plan, so a collapsed or zero-height canvas is the specific regression to look for.
    2. Confirm a "Reset View" button is visible near a bottom corner, sitting clear of the viewport edges, in blue (`#2563EB`), with its label reading exactly "Reset View".
    3. Orbit, pan, and zoom the camera well away from the opening view — deliberately end up somewhere disorienting.
    4. Click "Reset View" once. The camera must return to the exact view the page opened with.
    5. Orbit away again and click "Reset View" a second and third time — each click must work. A reset that fires once and then stops responding is the failure this step exists to catch.
    6. Click a nav-cube face, then click "Reset View" — the reset must still return to the opening view rather than to the cube's snapped view.
    7. Confirm the button is the only blue element in the DOM chrome and that the scene, not the button, still reads as the focal point of the frame.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe what did not work (e.g. "canvas collapsed after the Tailwind reset", "second click does nothing", "reset lands somewhere other than the opening view")</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

New symbols and paths created by **this plan** (excluded from cross-phase drift verification — none of these exist before Phase 1):

| Kind | Symbol / Path |
|---|---|
| File | `components.json`, `src/lib/utils.ts`, `src/components/ui/button.tsx` |
| File | `src/scene/camera-defaults.ts`, `src/scene/CameraResetListener.tsx` |
| File | `src/store/cellStore.ts`, `src/store/cellStore.test.ts` |
| File | `src/ui/ResetViewButton.tsx` |
| Constant | `DEFAULT_CAMERA_POSITION`, `DEFAULT_CAMERA_TARGET` (three-number tuples, metres) |
| Store | `useCellStore` — state `resetToken: number`; action `requestCameraReset(): void` |
| React component | `CameraResetListener` (in-Canvas, renders nothing), `ResetViewButton` (DOM overlay), `Button` (shadcn official block) |
| CSS custom property | `--space-xs` … `--space-3xl`, `--text-body`/`--text-label`/`--text-heading`/`--text-display`, `--leading-body`/`--leading-label`/`--leading-tight`, `--weight-regular`/`--weight-semibold`, `--color-dominant`/`--color-secondary`/`--color-accent`/`--color-destructive` |

Full phase-wide artifact inventory: see `01-SKELETON.md` → "Artifacts this phase produces".

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build machine | The design-system CLI installs a dependency set not covered by RESEARCH.md's legitimacy audit; install and build scripts execute locally |
| shadcn registry → source tree | The CLI writes registry-authored component source directly into `src/components/ui/`, where it is compiled and shipped like first-party code |
| build output → public internet | The rebuilt bundle is republished to the world-readable production URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | npm install of the shadcn/ui CLI dependency set (Tailwind, its Vite plugin, CVA, clsx, tailwind-merge, lucide-react, the animation package, the Radix slot primitive) | high | mitigate | These packages are absent from `01-RESEARCH.md`'s Package Legitimacy Audit, so the planner's fallback policy treats them as `[ASSUMED]`. The first task in this plan is a non-auto-approvable `blocking-human` checkpoint that verifies each package's registry page, canonical source org, and download volume before any install runs. After install, `package-lock.json` is committed so the resolved tree is frozen in git history, and the verify gate re-asserts the TypeScript 5.9 and Vite 7 pins to catch an install that silently moved a pinned major. If the CLI's real dependency set differs from the gated list, the executor re-raises the gate rather than proceeding. |
| T-01-08 | Tampering | Registry-authored `src/components/ui/button.tsx` written straight into the source tree | medium | mitigate | UI-SPEC "Registry Safety" restricts this phase to the shadcn official registry with `Button` as the only block and no third-party registry configured — the verify gate asserts `components.json` records no third-party registry. The generated file is read before commit rather than accepted unseen, and it lands as a reviewable diff in git rather than inside `node_modules`. |
| T-01-09 | Information Disclosure | The rebuilt static bundle republished to the public URL | low | accept | No secret exists in this phase to disclose: the client-side-only SPA has no backend, no API key, and no environment variable read anywhere in `src/`. The token layer this plan adds is design metadata that is public by construction — it is visible in the rendered page regardless. |
| T-01-13 | Denial of Service | Client bundle growth from Tailwind + Radix on the public URL | low | accept | Build-time bounded, single-user interview demo, entirely client-side; a degraded client affects only that visitor's own tab and there is no shared resource to exhaust. |
| T-01-04 | Spoofing / Repudiation / Elevation of Privilege | — | low | accept | No authentication, session, account, or access-control surface exists anywhere in this project (REQUIREMENTS.md Out of Scope: "Persistent backend/database/accounts"). ASVS L1 categories V2/V3/V4 are not applicable; recorded so the omission is deliberate rather than an oversight. |

ASVS level 1 · block-on severity: high · the one `high` row (T-01-07) is dispositioned `mitigate`, gated by a blocking human checkpoint that `workflow.auto_advance` cannot skip.
</threat_model>

<flagged_assumptions>
No `01-SPEC.md` exists for this phase, so the spec-less probe fallback supplied this plan's acceptance shape. The deterministic edge probe returned `unclassified`/`unresolved` for all six phase requirements, and `PROHIB_ABSENT=1`, so the two `must_haves.prohibitions` rows above were authored by the in-prompt prohibition-recall protocol rather than lifted from a spec. They carry **no wired check descriptor and are flagged unverified** — both are `judgment`-tier by construction ("does not discard user data", "does not move the camera unprompted") and route to the human checkpoint above, not to a mechanical gate.

| Req | Probe status | Assumption authored into `must_haves.truths` |
|---|---|---|
| SCENE-02 | unclassified / unresolved | "Reset the camera to a centered view with one action" is assumed to mean a single click of a single visible control, returning the camera to the framing the scene opened with — not a modifier-key chord, not a two-step confirm, and not an approximate "roughly centered" view. The stronger reading (that the restored view is *identical* to the opening view, not merely similar) is the one this plan implements, by deriving both from one exported constant. |
| SCENE-02 | unclassified / unresolved | "Centered view of the robot cell" is assumed to be the same default framing plan 01-01 chose for the initial camera. No source artifact names a second, distinct "reset" framing, and inventing one would make the requirement unverifiable. |

Design-system assumptions this plan introduces, surfaced rather than buried:
- **A5** — `01-UI-SPEC.md` states that `npx shadcn init` "is part of Phase 1's build work per `01-RESEARCH.md`'s installation sequence", but `01-RESEARCH.md`'s Installation block does **not** in fact list shadcn, Tailwind, or any of their dependencies. The UI-SPEC is the verified, signed-off design contract, so it wins and the install is planned here — but the packages it pulls in are consequently outside the researched legitimacy audit, which is exactly why task 1 of this plan is a blocking human gate rather than a silent install.
- **A6** — The shadcn CLI's exact dependency set and the exact files it rewrites vary by CLI version. The plan therefore instructs the executor to audit the CLI's actual changes (Vitest block, pinned majors, alias agreement) rather than assuming a fixed file list, and to re-raise the legitimacy gate if the installed package set differs from the gated one.
</flagged_assumptions>

<verification>
- Full Vitest suite green after the design-system CLI rewrites `vite.config.ts` — this is the specific regression that would silently orphan plan 01-02's kinematics gate.
- TypeScript 5.9 and Vite 7 pins intact after the CLI's install.
- All seventeen UI-SPEC tokens declared in `src/index.css`; `components.json` records the neutral preset and no third-party registry.
- Store suite green, including the strictly-increasing reset token that proves repeat resets fire.
- Static wiring assertion green: the initial camera, the reset listener, the store, and the button form one chain through the shared framing constant, and no per-frame value leaked into the store.
- Production URL returns 200 and serves its bundle after the push.
- Human checkpoint confirms SCENE-02 on the live URL — manual-only per VALIDATION.md ("small, purely visual interaction") — plus the Tailwind-preflight canvas regression check and the Accent-reservation check.
</verification>

<success_criteria>
- SCENE-02 satisfied: one click of one control returns the camera to the opening view, repeatably, on the deployed app.
- The UI-SPEC design contract is real code: neutral shadcn preset, the official Button block, the two-weight type scale, the four colour roles, and the seven spacing steps all declared once.
- Accent stays reserved: the nav cube (plan 01-01) and this CTA are the only surfaces carrying it.
- The minimal Zustand store exists with the shape Phases 5-8 extend, and carries no per-frame state.
- Plan 01-02's kinematics suite still runs green through the rewritten build config.
- The live deployment is redeployed and re-verified after this plan's work, per plan 01-01's handoff.
</success_criteria>

<output>
Create `.planning/phases/01-static-rig-kinematics-foundation/01-03-SUMMARY.md` when done. Record the exact export names of the store and the framing constants, the final alias prefix the shadcn CLI configured, and whether the CLI's dependency set matched the gated list — plan 01-04 extends the store and mounts its overlay against them.
</output>
